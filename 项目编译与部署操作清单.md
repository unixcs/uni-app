# 项目编译与部署操作清单

适用目录：`/opt/yoshop`

---

## 快速入口

- 只想重新编译 **uniapp 的 H5 / 微信小程序**：
  - 看：`uniapp一键同步编译参考.md`
- 想判断 **后端 / H5 / 微信小程序 / 商家后台 / 超管后台** 改动后该怎么处理：
  - 看本文档

## 文档定位

本文档是全项目总手册，覆盖：
- 后端 PHP 服务刷新
- uni-app 的 H5 / 微信小程序编译
- 商家后台前端构建与部署
- 超管后台前端构建与部署
- nginx / php-fpm / runtime 缓存处理

---

## 一、项目结构总览

### 1. 后端服务端
- 目录：`/opt/yoshop/yoshop2.0`
- 作用：
  - PHP 接口
  - 管理后台接口
  - 商家后台接口
  - H5 / 微信小程序共用 API
- 运行进程：
  - `nginx`
  - `php8.3-fpm`

### 2. 用户端 uni-app
- 目录：`/opt/yoshop/yoshop2.0-uniapp`
- 作用：
  - H5
  - 微信小程序
- H5 产物部署目录：`/opt/yoshop/yoshop2.0/public`
- 微信小程序产物目录：
  - `D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin`

### 3. 商家后台前端
- 目录：`/opt/yoshop/yoshop2.0-store`
- 作用：商家管理后台页面
- 部署目录：`/opt/yoshop/yoshop2.0/public/store`

### 4. 超管后台前端
- 目录：`/opt/yoshop/yoshop2.0-admin`
- 作用：超级管理员后台页面
- 部署目录：`/opt/yoshop/yoshop2.0/public/admin`

---

## 二、改代码后如何判断要不要编译

### 情况 A：只改后端 PHP
例如修改：
- `yoshop2.0/app`
- `yoshop2.0/config`
- `yoshop2.0/route`
- 订单、支付、用户、接口逻辑

**处理方式：**
- 不需要编译 H5
- 不需要编译微信小程序
- 不需要编译商家后台
- 不需要编译超管后台
- 只需要清缓存并重启服务

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx
```

---

### 情况 B：改了 uni-app 用户端
例如修改：
- `yoshop2.0-uniapp/pages`
- `yoshop2.0-uniapp/components`
- `yoshop2.0-uniapp/core`
- `yoshop2.0-uniapp/utils`

**处理方式：**
- 需要重新编译 H5
- 需要重新编译微信小程序

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

`dev:all` 会自动完成：
1. WSL 源码同步到 Windows 镜像
2. 编译 H5 并同步到 `yoshop2.0/public`
3. 编译微信小程序到 Windows 目录

---

### 情况 C：改了商家后台前端
例如修改：
- `yoshop2.0-store/src`

**处理方式：**
1. 重新构建商家后台前端
2. 覆盖到 `public/store`
3. 刷新静态资源

> 注意：`package.json` 里的 `SET NODE_OPTIONS=...` 是 Windows 写法，WSL 下不能直接用。

如果依赖未安装，先执行：

```bash
cd /opt/yoshop/yoshop2.0-store
npm install
```

构建命令：

```bash
cd /opt/yoshop/yoshop2.0-store
NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vue-cli-service build
```

部署命令：

```bash
rm -rf /opt/yoshop/yoshop2.0/public/store/*
cp -a /opt/yoshop/yoshop2.0-store/dist/. /opt/yoshop/yoshop2.0/public/store/
systemctl reload nginx
```

然后浏览器强刷页面。

---

### 情况 D：改了超管后台前端
例如修改：
- `yoshop2.0-admin/src`

**处理方式：**
1. 构建超管后台前端
2. 覆盖到 `public/admin`
3. 刷新静态资源

流程与商家后台相同。

---

### 情况 E：前后端都改了
如果同时改了：
- 后端 PHP
- uni-app 用户端
- 商家后台
- 超管后台

那就走全量流程。

---

## 三、全量更新流程

### 第 1 步：刷新后端

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx
```

---

### 第 2 步：重新编译用户端 uni-app

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

会生成：
- H5：同步到 `/opt/yoshop/yoshop2.0/public`
- 微信小程序：输出到 Windows 目录

---

### 第 3 步：重新编译商家后台

```bash
cd /opt/yoshop/yoshop2.0-store
NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vue-cli-service build
rm -rf /opt/yoshop/yoshop2.0/public/store/*
cp -a /opt/yoshop/yoshop2.0-store/dist/. /opt/yoshop/yoshop2.0/public/store/
systemctl reload nginx
```

---

### 第 4 步：重新编译超管后台（如有修改）

```bash
cd /opt/yoshop/yoshop2.0-admin
# 按商家后台同样方式构建并覆盖 public/admin
```

---

## 四、推荐操作模板

### 模板 1：只改后端

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx
```

### 模板 2：改了后端 + uni-app

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx

cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

### 模板 3：改了后端 + 商家后台

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx

cd /opt/yoshop/yoshop2.0-store
NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vue-cli-service build
rm -rf /opt/yoshop/yoshop2.0/public/store/*
cp -a /opt/yoshop/yoshop2.0-store/dist/. /opt/yoshop/yoshop2.0/public/store/
systemctl reload nginx
```

### 模板 4：全量更新

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx

cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all

cd /opt/yoshop/yoshop2.0-store
NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vue-cli-service build
rm -rf /opt/yoshop/yoshop2.0/public/store/*
cp -a /opt/yoshop/yoshop2.0-store/dist/. /opt/yoshop/yoshop2.0/public/store/

systemctl reload nginx
```

---

## 五、常见问题

### 1. 改了后端为什么页面没变化？
常见原因：
- `runtime` 缓存没清
- `php8.3-fpm` 没重启
- 浏览器缓存旧接口响应

### 2. 改了商家后台 / H5 为什么还是旧页面？
常见原因：
- 构建产物没有真正覆盖到 `public` / `public/store`
- 浏览器缓存了旧 `index.html`
- 微信开发者工具打开的不是最新小程序目录

### 3. 静态文件更新后要不要重启 nginx？
通常不是必须。

但建议：
- 部署后执行 `systemctl reload nginx`
- 浏览器执行强刷

---

## 六、一句话规则

- 改 **后端 PHP** → 清 `runtime` + 重启 `php8.3-fpm/nginx`
- 改 **uni-app 用户端** → 重新编译 **H5 + 微信小程序**
- 改 **商家后台前端** → build `yoshop2.0-store` + 覆盖 `public/store`
- 改 **超管后台前端** → build `yoshop2.0-admin` + 覆盖 `public/admin`
