# 微信小程序虚拟支付开发总手册

> 目标：把当前 `yoshop` 项目里分散在编译、DevTools、自动化、虚拟支付几套文档中的有效信息重新整理成一份主入口文档。后续不管是在当前项目里继续联调，还是新建一个独立目录的小程序虚拟支付项目，都先从这里开始。

## 1. 先记住这套系统的真实结构

这套链路不是“一个目录 + 一个 IDE”就能跑通的。当前已经稳定下来的工作方式是：

1. WSL 负责日常开发和 H5 编译
2. Windows 镜像目录负责微信小程序编译
3. HBuilderX CLI 负责从 Windows 镜像项目产出 `mp-weixin`
4. 微信开发者工具只打开 Windows 侧编译产物
5. 虚拟支付验收必须靠真实环境，不能把开发者工具接口检查当成支付闭环验收

当前固定路径：

- WSL 前端源码：`/opt/yoshop/yoshop2.0-uniapp`
- Windows 镜像目录：`/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp`
- Windows 项目路径：`D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp`
- HBuilderX CLI：`/mnt/d/Program/tools/HBuilderX/cli.exe`
- 微信开发者工具 CLI：`/mnt/d/Program/soft/wechattools/cli.bat`
- 微信小程序编译产物：`D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin`
- 后端 PHP 项目：`/opt/yoshop/yoshop2.0`

核心原则只有一句：`WSL 改源码，Windows 编小程序，DevTools 只看 Windows 产物。`

## 2. 当前项目里哪些东西已经能直接复用

### 2.1 编译与同步脚本

`/opt/yoshop/yoshop2.0-uniapp/package.json` 里已经有三条主命令：

- `npm run dev:all`：同步源码到 Windows 镜像 + 编译 H5 + 编译 mp-weixin
- `npm run build:h5:sync`：仅编译 H5 并同步到 `yoshop2.0/public`
- `npm run build:mp-weixin`：调用 HBuilderX CLI 编译微信小程序

对应脚本职责：

- `scripts/dev-all.cjs`：总入口，先 `rsync`，再 H5，再 mp-weixin
- `scripts/build-h5-sync.cjs`：H5 编译并同步到后端 `public`
- `scripts/build-mp-weixin.cjs`：调用 Windows 侧 HBuilderX CLI，必要时临时覆盖 `config.js` 的 `apiUrl`

### 2.2 微信开发者工具辅助脚本

这些脚本已经存在，不要重复造轮子：

- `scripts/reset-wechat-devtools-manual.sh/.cmd`
- `scripts/upload-wechat-experience.sh/.cmd`
- `scripts/patch-wechat-devtools-cli-bridge.sh/.cmd`
- `scripts/run-wechat-virtual-sandbox-e2e.cjs`
- `scripts/watch-next-wechat-virtual-payment-retry.sh`

其中 `watch-next-wechat-virtual-payment-retry.sh` 已经能串起三件事：

1. 重置当前 DevTools 会话
2. 上传最新体验版
3. 等待下一次 Android 真机支付重试并调用 `php think virtual-payment:watch-live`

`run-wechat-virtual-sandbox-e2e.cjs` 则更偏向“自动化收证工具”：

1. 检查 workspace / Windows 镜像 / 编译产物三处 `apiUrl` 是否对齐
2. 通过 DevTools CLI 和 automator 打开小程序项目
3. 拦截和记录 `wx.requestVirtualPayment` 的真实入参
4. 输出一份本地 evidence 文件，方便回放本次沙箱链路

### 2.3 当前项目已经有可复用签名逻辑

这是这次最重要的结论之一：当前项目不是“只有文档、没有实现”，而是已经有两层可直接复用的签名逻辑，外加一条配套的 `session_key` 回源链路。

1. 小程序前端支付参数签名：
   `yoshop2.0/app/api/service/cashier/Payment.php`
2. 服务端调用微信 xpay 接口的签名：
   `yoshop2.0/app/common/library/wechat/VirtualPayment.php`
3. 配套的 `session_key` 回源：
   `yoshop2.0/app/common/model/UserOauth.php`

当前实现口径：

- `paySig = HMAC_SHA256("requestVirtualPayment&" + signData, appKey)`
- `signature = HMAC_SHA256(signData, session_key)`
- `pay_sig = HMAC_SHA256(uri + "&" + body, appKey)` 仅用于当前封装里带签名的 xpay 接口

例外要单独记住：`/xpay/notify_provide_goods` 在当前实现里不带 `pay_sig`，只带 `access_token`。

这套实现与仓库内现有虚拟支付资料和微信官方文档口径一致，因此默认应作为新项目的复用源，而不是重新写一套签名算法。只有在平台规则变化或新项目架构完全不同的情况下，才允许做薄封装迁移。

### 2.4 当前可直接用的 skill 与工具

如果仍然在 Codex / 自动化环境里协作，当前最有用的是这两类：

1. `virtual-payment` skill：用于快速回忆微信虚拟支付接入顺序、签名口径、常见错误码、回调与发货链路
2. 本仓库里的脚本工具箱：用于编译、同步、DevTools 上传、automator 抓参、真机重试收证

建议把它们分工看待：

- skill 负责“方法论和规范口径”
- 本地脚本负责“当前项目里的实际执行”

## 3. 日常开发的标准闭环

### 场景 A：只改当前 `yoshop` 前端

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

这条命令会完成：

1. WSL 源码同步到 Windows 镜像目录
2. H5 编译并同步到 `/opt/yoshop/yoshop2.0/public`
3. Windows 镜像目录编译出 `mp-weixin`

然后分别验证：

- 浏览器：`http://localhost/#`
- 微信开发者工具：打开 `D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin`

### 场景 B：只改后端 PHP

```bash
/opt/yoshop/scripts/repair-local-runtime.sh --clear-cache
systemctl restart php8.3-fpm nginx
```

如果没有改 uni-app，不需要重新编译小程序。

### 场景 C：虚拟支付专项联调

推荐顺序：

1. 改前端/后端代码
2. `npm run dev:all`
3. 打开微信开发者工具确认产物已更新
4. 需要体验版时，先执行 DevTools 上传辅助脚本
5. Android 真机扫码或进入体验版
6. 触发虚拟支付
7. 同时观察后端日志、支付审计日志、`virtual-payment:watch-live` 证据输出

这里不要把 `php think service-order:e2e check --check mp-weixin` 当成“虚拟支付已经验收”的证明。它目前更接近接口视角检查，不会真实拉起 `wx.requestVirtualPayment`。

## 4. HBuilderX、DevTools、CLI 之间怎么串起来

### 4.1 为什么必须用 Windows 镜像目录

历史上最容易乱掉的点，就是把 WSL 路径直接交给 Windows 编译器或 DevTools。当前仓库已经验证过，这么做很容易出现：

- watch 失败
- 产物不是最新
- DevTools 打开旧缓存
- `\wsl.localhost\...` 路径兼容问题

所以现在统一要求：

- 源码只在 WSL 主目录改
- 小程序编译只吃 Windows 镜像目录
- DevTools 只打开 Windows 侧 `unpackage/dist/dev/mp-weixin`

### 4.2 命令关系

最常用的是这一条：

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

当它失效时，再拆分为：

```bash
rsync -a --delete --exclude node_modules --exclude dist --exclude unpackage --exclude .git --exclude .vite /opt/yoshop/yoshop2.0-uniapp/ /mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/
cd /opt/yoshop/yoshop2.0-uniapp
npm run build:h5:sync
cd /mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp
npm run build:mp-weixin
```

### 4.3 体验版与真机联动

需要真实 Android 验收时，优先用现有脚本：

1. `reset-wechat-devtools-manual.*`：清理 DevTools 会话状态
2. `upload-wechat-experience.*`：上传最新体验版
3. `watch-next-wechat-virtual-payment-retry.sh`：等待下一次真实支付尝试并自动收证

推荐把这套脚本当成“支付专项调试工具箱”，而不是散落的临时脚本。

## 5. 当前项目虚拟支付调用链

### 5.1 业务链路

当前项目中，一笔虚拟支付至少经过这些环节：

1. 前端发起“创建虚拟支付参数”请求
2. 后端在 `Payment.php` 组装 `signData`
3. 后端优先用本次 `wx.login` 换取新 `session_key`，失败后才回退到当前登录态缓存
4. 后端计算 `paySig` 和 `signature`
5. 前端调用 `wx.requestVirtualPayment`
6. 微信侧完成支付后，再进入查单、发货、回调、补偿逻辑

### 5.2 当前前端真正传给 `wx.requestVirtualPayment` 的字段

当前项目最终调用字段只有四个：

- `mode`
- `signData`
- `paySig`
- `signature`

其中 `signData` 的对象主体由后端生成，当前联调用例里核心字段包括：

- `offerId`
- `buyQuantity`
- `env`
- `currencyType`
- `productId`
- `goodsPrice`
- `outTradeNo`
- `attach`

这一块的专项说明仍以 [虚拟支付/1.虚拟支付api.md](/opt/yoshop/虚拟支付/1.虚拟支付api.md) 和 [虚拟支付/wx.requestVirtualPayment.md](/opt/yoshop/虚拟支付/wx.requestVirtualPayment.md) 为准。

### 5.3 当前签名逻辑怎么复用

新项目不要重新设计签名，直接复用当前思路：

1. 继续由服务端生成 `signData`
2. 继续由服务端持有 `appKey`
3. 继续由服务端优先使用本次 `loginCode` 刷新 `session_key`，失败后再回退缓存
4. 继续返回 `paySig`、`signature` 给前端
5. 服务端调微信 xpay 接口时，复用 `VirtualPayment.php` 的现有封装：查单/退款走 `signedPost()`，`notifyProvideGoods()` 保持当前“不带 pay_sig、只带 access_token”的例外口径

如果要在“新目录下的独立小程序”里搬过去，建议只做三类调整：

- 替换新的 `appId / offerId / appKey`
- 替换新的订单模型和业务商品字段映射
- 替换新的用户登录态持久化位置

签名公式本身不要改。

## 6. `accepted runtime path` 与 `payer / operator` 应该怎么拿

### 6.1 accepted runtime path

它不是微信单独吐给你的一个现成字段，而是要在支付前主动采集运行时上下文。按当前代码真实实现，最少应记录：

- `envVersion`
- `appId`
- `platform`
- `system`
- `wechatVersion`
- `sdkVersion`
- `scene`

在当前项目里，建议前端把 `runtimeContext` 连同创建支付参数请求一起传给后端，由后端写入支付审计日志或扩展字段。

这里不要再包一层 `acceptedRuntimePath` 对象。当前项目真实传输和落日志的字段名就是 `runtimeContext`，而 `accepted runtime path` 只是这组字段在验收语境里的业务叫法。

当前验收口径必须能证明：

- 这次确实是 `Android` 真机
- 当前 `env` 是否为沙箱 `1` 或正式 `0`，这部分要结合支付参数里的 `signData.env` 一起看
- 当前微信版本和 SDK 版本真实可追溯

### 6.2 payer

`payer` 指当前真正发起支付的小程序登录用户。这个身份标签以后端为准，不要完全信前端传值。建议从：

1. 当前登录 token
2. 商城 `user_id`
3. 用户手机号
4. `user_oauth` 中的 `openid`

反推出结构化 `payerContext`。

`session_key` 只用于服务端签名，不建议作为 `payerContext` 对外扩散或落到验收身份标签里。

### 6.3 operator

`operator` 指谁在操作这次测试。它不是 `wx.requestVirtualPayment` 自动返回值，通常需要人工补录。

推荐口径：

- 同一人自测：`sameAsPayer=true`
- 有代测/上传体验版/生成二维码人员：前端 UI 或后台表单手工录入 `operatorContext`

详细字段建议见 [虚拟支付/1.虚拟支付api.md](/opt/yoshop/虚拟支付/1.虚拟支付api.md)。

## 7. 新目录下独立虚拟支付小程序怎么规划

如果要新建一个更干净的虚拟支付小程序目录，建议按“复用能力 + 重新接线”的思路做。

### 7.1 直接复用

- 当前虚拟支付签名口径
- 当前支付参数结构设计
- 当前 Android 真机验收路径
- 当前 Windows 镜像编译和 DevTools 联动方式
- 当前调试收证脚本思路

### 7.2 重新接线

- 新小程序 `appId`
- 新虚拟支付 `offerId / appKey`
- 新后端接口域名和路由
- 新订单模型
- 新用户登录态和 `session_key` 存储
- 新项目自己的日志与审计表

### 7.3 推荐落地步骤

1. 先定义新项目的最小支付链路：登录、下单、签名、拉起支付、查单、发货
2. 先移植后端签名与查单能力，再接前端页面
3. 先跑 `Android + env=1 + 低价商品` 的沙箱路径
4. 真机跑通后，再考虑体验版、自动化和回归脚本

## 8. 常见问题排查顺序

### 8.1 页面是旧的

优先检查：

1. 是否真的执行过 `npm run dev:all`
2. Windows 镜像目录是否已同步最新源码
3. DevTools 打开的是否是 Windows 产物目录
4. 是否还残留旧的 DevTools 会话或 automator mock

### 8.2 拉不起虚拟支付或一直 `capture only`

优先检查：

1. 是否还在 `miniprogram-automator` mock 模式
2. 是否重新打开了普通 DevTools 项目会话
3. 是否使用了旧编译产物
4. 是否误连到旧 `apiUrl`

### 8.3 签名报错或支付失败

优先检查：

1. `signData` 是否和实际传入微信的一字不差
2. `appKey` 是否取对了沙箱/正式环境
3. `session_key` 是否失效
4. 商品 `productId`、`goodsPrice` 是否与后台配置一致
5. 当前路径是否满足 Android 真机验收口径

### 8.4 明明下单了，但验收证据不完整

优先补齐：

1. `runtimeContext`
2. `payerContext`
3. `operatorContext`
4. 后端 trace / watch-live 证据
5. 体验版上传时间、真机重试时间与订单号对应关系

## 9. 参考资料地图

### 9.1 当前仓库内优先阅读顺序

1. [项目编译与部署操作清单.md](/opt/yoshop/项目编译与部署操作清单.md)
2. [uniapp一键同步编译参考.md](/opt/yoshop/uniapp一键同步编译参考.md)
3. [开发流程.md](/opt/yoshop/开发流程.md)
4. [微信开发者工具.md](/opt/yoshop/微信开发者工具.md)
5. [自动化测试.md](/opt/yoshop/自动化测试.md)
6. [虚拟支付/0虚拟支付参考.md](/opt/yoshop/虚拟支付/0虚拟支付参考.md)
7. [虚拟支付/0.小程序虚拟支付接入指引.md](/opt/yoshop/虚拟支付/0.小程序虚拟支付接入指引.md)
8. [虚拟支付/1.虚拟支付api.md](/opt/yoshop/虚拟支付/1.虚拟支付api.md)
9. [虚拟支付/wx.requestVirtualPayment.md](/opt/yoshop/虚拟支付/wx.requestVirtualPayment.md)
10. [其他/虚拟支付验收故障诊断报告.md](/opt/yoshop/其他/虚拟支付验收故障诊断报告.md)
11. [其他/虚拟支付当前阻力说明_20260703.md](/opt/yoshop/其他/虚拟支付当前阻力说明_20260703.md)
12. [openspec/changes/add-mp-virtual-payment-service-orders/references/virtual-payment-sandbox-checklist.md](/opt/yoshop/openspec/changes/add-mp-virtual-payment-service-orders/references/virtual-payment-sandbox-checklist.md)

### 9.2 微信官方资料

- 虚拟支付概览：<https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment.html>
- 虚拟支付开发接入：<https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment/devplan.html>
- `wx.requestVirtualPayment` API：<https://developers.weixin.qq.com/miniprogram/dev/api/payment/wx.requestVirtualPayment.html>
- iOS 接入说明：<https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment/ios.html>

## 10. 最后定一条执行口径

后续做微信小程序虚拟支付时，统一按下面这条最小闭环执行：

1. 在 WSL 改代码
2. `npm run dev:all`
3. DevTools 打开 Windows 编译产物
4. 需要真机时上传体验版
5. Android 真机发起支付
6. 记录 `accepted runtime path`
7. 后端自动补 `payerContext`
8. 需要时手工记录 `operatorContext`
9. 复用现有签名逻辑，不重写公式
10. 以真实支付闭环和后端证据为准，不以单纯接口检查为准

如果后面要再拆成“当前项目版”和“新项目模板版”两份文档，也应以这份手册为母版，而不是重新从旧文档碎片里拼。
