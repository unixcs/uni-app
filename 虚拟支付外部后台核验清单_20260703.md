# 虚拟支付外部后台核验清单

时间: 2026-07-03 21:10 CST

## 已证实的本地事实

- 小程序前端已调用 `wx.requestVirtualPayment`
- 后端已稳定走到 `provider=virtual` / `platform=wechat_virtual`
- 签名链路当前未出现官方签名错误码 `-15005/-15006/-15007/-15013/-15016`
- Android 真机 + 体验版 已经不是 DevTools 预览态
- 微信远端会真实创建订单, 但订单很快被关闭

关键证据:

- 最新体验版监听证据:
  [watch-20260703210147-070922.json](/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-experience-retry/watch-20260703210147-070922.json)
- 最新沙箱审计证据:
  [sandbox-check-20260703210210-301427.json](/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10173/sandbox-check-20260703210210-301427.json)
- 最新请求链路追踪:
  [20260703.jsonl](/opt/yoshop/yoshop2.0/runtime/api/codex-evidence/virtual-payment-attempt-trace/20260703.jsonl)

这次真实 Android 体验版单据:

- `trade_id=10173`
- `order_id=10361`
- `out_trade_no=331539720188296576`

远端状态变化:

- `21:00:30 status=0`
- `21:00:31 status=1`
- `21:00:33 status=6`

这说明:

- 微信上游接受了下单
- 订单进入过支付中间态
- 但在成功支付前被上游关闭

## 官方文档约束

1. `wx.requestVirtualPayment` 官方错误码里:
   - `-2` = 支付取消
   - `-15010` = 道具 `productId` 未发布
   - `-15011` = 现网版本 `env` 不能填 `1`
   - `-15012` = 调用米大师失败导致关单
   - `-15014` = 道具/代币发布未生效, 大概 10 分钟后生效
   来源:
   - https://developers.weixin.qq.com/miniprogram/dev/api/payment/wx.requestVirtualPayment.html

2. 虚拟支付官方说明明确区分:
   - `env=0` = 正式环境
   - `env=1` = 沙箱环境
   - `appKey` 必须与 `env` 匹配
   来源:
   - https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment.html

3. 官方能力说明明确提到:
   - 道具需要在小程序后台的虚拟支付模块里进行“上传到开发版本 / 发布到现网版本”
   - Android / 鸿蒙 / Windows 适用虚拟支付主文档
   来源:
   - https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment.html

4. iOS 官方文档明确:
   - iOS 不支持沙箱环境
   - iOS 最低支付金额 1 元
   来源:
   - https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment/ios.html

## 现在最该核验的微信后台项

### A. 道具 `vip0` 的发布状态

在微信公众平台 / 小程序后台 / 虚拟支付 / 道具管理 中确认:

- `vip0` 是否存在
- `vip0` 是否上传到了开发版本
- `vip0` 是否已经发布
- 如果刚发布过, 是否已等待至少 10 分钟再测

重点:

- 官方错误定义里, `-15010` 和 `-15014` 都指向道具发布链路
- 我们当前虽然没直接拿到这两个前端错误码, 但远端快速关单与这条链路高度相关

### B. 当前扫码支付账号是否是微信侧允许的测试账号

确认当前实际付款微信号:

- 是否就是虚拟支付沙箱允许的测试账号
- 是否与体验成员/开发成员/虚拟支付测试账号配置一致

重点:

- 我们本地当前固定命中的商城用户是 `user_id=10004`
- 对应 `openid` 已唯一化, 本地不再混号
- 但微信侧是否允许该付款号在当前 `offerId` / 沙箱道具下完成支付, 仍需后台核实

### C. `offerId=1450568898` 与当前小程序、商户、道具是否在同一套虚拟支付主体下

在“基础配置”和“商户管理”中确认:

- `AppID = wxbf57e6f2ba72b712`
- `OfferID = 1450568898`
- 当前虚拟支付商户号已完成签约、未受限
- `vip0` 属于这个 `OfferID` 下的道具, 不是旧主体、旧商户或别的小程序残留

### D. 虚拟支付商户状态是否受限

官方错误表里有:

- `-15017` 商家涉嫌违规, 收款受限
- `-15019` 商户受限

虽然我们当前没有直接拿到这两个码, 但仍需登录:

- 微信商户平台
- 微信支付商家助手

确认虚拟支付商户没有风控或收款限制

### E. 体验版是否真是最新包

本地已成功上传:

- `vp-sandbox-2026.0703.2058`

需要在微信后台确认:

- 手机扫码进入的确实是这个体验版
- 不是旧体验版
- 不是 live preview

## 本地已排除项

- 普通微信支付串路
- DevTools 旧 `127.0.0.1:8088` API 漂移
- `automator capture only` 旧会话噪音作为唯一解释
- iOS 沙箱限制
- 本地 session_key 重复绑定
- 本地签名算法方向性错误
- 体验版/Android 真机未真正起单

## 结论

截至 2026-07-03 21:10 CST, 本地代码链路已经证明:

- 能建商城订单
- 能建真实 `wechat_virtual` 交易
- 能由 Android 真机体验版实际触发微信远端建单

但还没有证据表明:

- 微信上游允许该沙箱订单完成支付

因此当前最可能的剩余根因已收敛到微信外部后台状态:

- 道具 `vip0` 的开发/发布状态
- 沙箱测试支付账号资格
- `offerId/appid/商户/道具` 主体一致性
- 商户风控或收款限制
