# Tasks

## 1. Configuration Baseline

- [x] 1.1 确认虚拟支付服务端配置来源，支持开关、环境、OfferID、沙箱 AppKey、现网 AppKey、商户号、回调基础域名
- [x] 1.2 保留现有 `wxapp_setting` 作为小程序 `app_id/app_secret` 配置来源，不重复造配置体系
- [x] 1.3 将当前仓库文档中的现网密钥迁移到部署密钥或服务端配置，不再作为源码明文依赖
- [x] 1.4 准备沙箱联调参数并确认沙箱为首期验收环境
- [x] 1.5 补齐通知、查单、退款接口契约与样例；未补齐前冻结对应开发任务

## 2. Goods Mapping Model

- [x] 2.1 在商品表增加虚拟支付字段：是否启用、`productId`、平台价格快照
- [x] 2.2 在后台商品编辑页增加虚拟支付配置项
- [x] 2.3 保存商品时校验：仅服务商品可启用虚拟支付
- [x] 2.4 保存商品时校验：商城售价必须与平台价格快照一致
- [x] 2.5 首期固定商品级一对一 `productId` 绑定，不实现 SKU 多映射

## 3. Settlement Constraints

- [x] 3.1 保持仅支持立即购买，不支持购物车和混合结算
- [x] 3.2 结算虚拟支付商品时，若存在任何改价输入或最终应付金额不等于价格快照，则直接拒绝支付
- [x] 3.3 不为虚拟支付商品适配复杂营销改价链路

## 4. Trade Persistence

- [x] 4.1 评估复用现有支付流水模型；若不足，再新增虚拟支付最小事实表
- [x] 4.2 记录订单、用户、环境、`productId`、价格、`outTradeNo` 与平台返回标识
- [x] 4.3 为支付通知与退款通知保留原始报文快照
- [x] 4.4 为重复通知保留必要追踪字段

## 5. Backend Payment Capability

- [x] 5.1 保持外部支付方式仍为 `wechat`，仅在服务端支付分发器内部增加虚拟支付路由
- [x] 5.2 实现虚拟支付下单参数生成接口
- [x] 5.3 实现 `signData` 组装逻辑
- [x] 5.4 实现 `paySig` 计算逻辑
- [x] 5.5 实现 `signature` 生成逻辑
- [x] 5.6 按环境正确切换沙箱和现网 `AppKey`
- [x] 5.7 生成规范且唯一的虚拟支付交易号 `outTradeNo`

## 6. Cashier Integration

- [x] 6.1 在小程序收银台识别“启用虚拟支付的服务订单”
- [x] 6.2 让该类订单继续显示现有微信支付名称和图标
- [x] 6.3 对该类订单绕开普通 `uni.requestPayment` 微信支付分支
- [x] 6.4 接入 `wx.requestVirtualPayment`
- [x] 6.5 处理支付取消、失败、超时的前端提示
- [x] 6.6 支付完成后刷新订单状态，不把前端 success 当成唯一成功依据

## 7. Order Reuse

- [x] 7.1 复用现有服务订单建单逻辑
- [x] 7.2 保证虚拟支付成功后订单进入 `待联系`
- [x] 7.3 复用后台“开始服务”将订单推进到 `服务中`
- [x] 7.4 复用后台“完成服务”将订单推进到 `已完成`
- [x] 7.5 保持“商家确认 = 服务开始”，不新增中间状态
## 8. Notification and Query Reliability

- [x] 8.1 明确订单表是支付成功的业务真源，交易表只记录平台事实
- [x] 8.2 实现虚拟支付支付结果通知处理
- [x] 8.3 实现通知验签与来源校验
- [x] 8.4 实现按 `outTradeNo` 的幂等处理
- [x] 8.5 实现主动查单能力，作为前端 success 丢失时的兜底
- [x] 8.7 记录通知次数、最近通知时间和异常日志

## 9. Refund Reuse

- [x] 9.1 复用“未开始服务自动退款”规则
- [x] 9.2 复用“服务中用户申请 + 商家审核退款”规则
- [x] 9.3 复用“已完成后不可退款”规则
- [x] 9.4 将退款执行接入微信虚拟支付退款接口
- [x] 9.5 实现退款结果查询与异步通知确认
- [x] 9.6 保证自动退款和审核退款共用同一套退款执行路径

## 10. Admin Experience

- [x] 10.1 在后台订单详情展示是否虚拟支付、交易号、`productId`、环境、支付状态、退款状态
- [x] 10.2 保留现有服务订单操作入口，不改商家使用习惯

## 11. Validation

- [ ] 11.1 沙箱验证 `0.01` 商品支付成功
- [x] 11.2 验证支付成功后订单进入 `待联系`
- [x] 11.3 验证后台开始服务后订单进入 `服务中`
- [x] 11.4 验证后台完成服务后订单进入 `已完成`
- [x] 11.5 验证未开始服务时退款为自动退款
- [x] 11.6 验证服务中退款需要商家审核
- [x] 11.7 验证已完成服务后不可退款
- [x] 11.8 验证重复通知不会重复履约或重复退款
- [x] 11.9 验证前端 success 丢失时仍可通过通知或查单完成状态收敛

## Validation Evidence

> Notes: this section is chronological. Earlier bullets capture intermediate blocked states at that time; the latest dated bullets supersede older readiness/pending conclusions when they disagree.

- 2026-07-01 `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-final"` passed.
- Evidence: `/opt/yoshop/runtime/service-order-e2e-final/check-20260701185843.json` and `/opt/yoshop/runtime/service-order-e2e-final/real-order-20260701185843.json`.
- Cleanup: `php think service-order:e2e cleanup --evidence-dir "/opt/yoshop/runtime/service-order-e2e-final"` removed 7 orders, 7 goods rows, and 4 refund rows.
- Local virtual-payment sandbox is not enabled in this environment: `virtual_payment.enabled=0`, app keys and message-push token are empty, and no `wechat_virtual` payment trades exist locally. Therefore 11.1, 11.8, and 11.9 remain pending until real WeChat sandbox evidence is available.
- 2026-07-01 added `php think virtual-payment:sandbox-check` to codify the real sandbox readiness gate without mutating business data.
- Preflight evidence: `/opt/yoshop/runtime/virtual-payment-sandbox-check/sandbox-check-20260701190900.json`. It confirms goods `10001` is mapped to `vip0` at `0.01`, but runtime virtual-payment config, message-push token, test-user `MP-WEIXIN` openid/session_key, and real `wechat_virtual` trade evidence are still missing.
- Regression after adding the preflight command passed: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-sandbox-check"`.
- Cleanup after that regression removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-01 hardened `php think virtual-payment:sandbox-check` with real-trade audit options: `--expect-paid`, `--expect-pending-contact`, `--expect-duplicate-notify`, `--expect-query-evidence`, `--expect-no-duplicate-refund`, and `--expect-provide-goods-idempotent`.
- Current readiness evidence: `/opt/yoshop/runtime/virtual-payment-sandbox-check-current/sandbox-check-20260701200219.json`. It still fails before real sandbox execution for the same external gates: virtual-payment runtime config, message-push token, test-user `MP-WEIXIN` openid/session_key, and real `wechat_virtual` trade evidence are missing.
- Current regression passed after hardening the sandbox audit command: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-current"`.
- Cleanup after current regression removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-01 added `php think virtual-payment:local-e2e` as a local, non-WeChat regression fixture for notification idempotency, query fallback convergence, and provide-goods dispatch idempotency. This does not replace real WeChat sandbox acceptance for 11.1, 11.8, or 11.9.
- Local virtual-payment regression passed: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-final"`. Evidence: `/opt/yoshop/runtime/virtual-payment-local-e2e-final/check-20260701202443.json`.
- Local trade audit evidence: `/opt/yoshop/runtime/virtual-payment-local-e2e-current/audit-duplicate/sandbox-check-20260701201415.json` and `/opt/yoshop/runtime/virtual-payment-local-e2e-current/audit-query/sandbox-check-20260701201415.json`. Trade-level checks passed, while real sandbox readiness still failed due missing external runtime config and MP-WEIXIN test-user OAuth/session data.
- Cleanup after local virtual-payment regression removed 2 orders, 2 trades, 2 goods rows, and 0 refund rows. Cleanup now only rolls back sales/stock for generated orders that actually reached paid status.
- Service-order regression after adding local virtual-payment E2E passed: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-local-vp-final"`.
- Cleanup after that service-order regression removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-01 added deployment documentation for `[VIRTUAL_PAYMENT]` `.env` configuration and operations checks, and masked `message_push_token` in sandbox readiness evidence.
- Final readiness evidence for this pass: `/opt/yoshop/runtime/virtual-payment-sandbox-check-final/sandbox-check-20260701203614.json`. It still fails only on external gates: virtual-payment runtime config, message-push token, test-user `MP-WEIXIN` openid/session_key, and real `wechat_virtual` trade evidence.
- Final local virtual-payment regression passed: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-after-docs"`. Cleanup removed 2 orders, 2 trades, 2 goods rows, and 0 refund rows.
- Final service-order regression passed: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-docs"`. Cleanup removed 7 orders, 7 goods rows, and 4 refund rows.
- Residual local fixture check returned `local_e2e_orders=0` and `local_e2e_trades=0`.
- 2026-07-02 fixed virtual-payment trade replay persistence so repeated unpaid payment attempts keep historical `out_trade_no` rows instead of overwriting them. This protects late notify/query convergence for the original trade.
- 2026-07-02 hardened duplicate-payment refund handling for virtual trades: repeated loser-trade callbacks now reuse the same refund-in-flight state instead of reissuing refunds, and trade-only duplicate-payment refunds can converge through notify/timer without requiring a business `order_refund` row.
- Local regression after replay/refund hardening passed: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-after-refund-guard"`. Evidence: `/opt/yoshop/runtime/virtual-payment-local-e2e-after-refund-guard/check-20260702134634.json`.
- 2026-07-02 replenished sandbox test goods `10001` stock after prior runs exhausted it (`stock_total=20`, SKU `0` stock=`20`) so acceptance checks could continue.
- Current non-virtual service-order regression still passes after the replay/refund hardening: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-refund-guard"`. Cleanup removed 18 orders, 18 goods rows, and 4 refund rows.
- Current sandbox readiness now passes with the updated `.env`, notify config, user session data, and replenished stock: `/opt/yoshop/runtime/virtual-payment-sandbox-check-after-refund-guard/sandbox-check-20260702134816.json`.
- Real WeChat sandbox acceptance remains pending for 11.1, 11.8, and 11.9. Latest user-observed DevTools/native behavior is still external-state blocked by QR scan payment returning `系统繁忙，请稍后再试`, while the DevTools page later shows `requestVirtualPayment:fail automator capture only`, which is not sufficient evidence of a real paid trade.
- 2026-07-02 first-principles audit of the latest “successful” sandbox scans proved those payments were ordinary WeChat pay, not virtual pay: `yoshop_payment_trade.trade_id in (10045,10046,10047)` had empty `platform/product_id/env`, and the user-facing merchant name matched the ordinary pay merchant.
- Root cause of that false-positive acceptance path was environment drift, not the virtual-payment code path itself: goods `10001` had been changed to `vp_enabled=0`, `vp_product_id=''`, `vp_price_snapshot=0`, so the backend correctly fell back to ordinary WeChat pay for `cashier/orderPay`.
- Restored the real sandbox acceptance fixture by resetting goods `10001` to `vp_enabled=1`, `vp_product_id='vip0'`, `vp_price_snapshot=1`; direct MP-WEIXIN verification now again returns `provider=virtual` and `platform=wechat_virtual` for goods `10001`.
- Post-restore readiness evidence: `/opt/yoshop/runtime/virtual-payment-sandbox-check-after-goods-restore/sandbox-check-20260702142751.json`.
- Post-restore DevTools evidence confirms a fresh MP-WEIXIN cashier session can again reach virtual-payment parameters for goods `10001`: `/opt/yoshop/runtime/service-order-e2e/wechat-virtual-platform-after-restore.json` and `/opt/yoshop/runtime/service-order-e2e/wechat-virtual-sandbox-live-scan-now.json`.
- 2026-07-02 used the referenced `beeyang0/miniprogram-VirtualPayment` skill/repo to re-check sandbox constraints from first principles. It matches the current blocker: iOS does not support virtual-payment sandbox `env=1`, and iOS virtual payment also has a `1元` minimum. This means a `0.01` sandbox acceptance attempt cannot succeed on an iPhone scanner even if the code path is correct.
- 2026-07-02 confirmed the user-facing `requestVirtualPayment:fail automator capture only` string was not coming from the runtime payment code path anymore; it only remained in local guidance/docs. The main sandbox automation script no longer injects that mock-fail behavior into the real acceptance path.
- 2026-07-02 rebuilt the actual Windows `mp-weixin` output consumed by WeChat DevTools after adding an iOS sandbox guard in `yoshop2.0-uniapp/core/payment/wechat.js`, then cleaned project compile/session/storage/network caches with the WeChat DevTools CLI. Before this rebuild, `/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/unpackage/dist/dev/mp-weixin/core/payment/wechat.js` was still stale and did not contain the new iOS/env=1 rejection branch; after rebuild it now contains the explicit guard message `iOS 设备不支持虚拟支付沙箱环境，请改用 Android 真机联调或切换正式环境`.
- Latest readiness evidence after rebuild/cache clean still passes locally: `/opt/yoshop/runtime/virtual-payment-sandbox-check-after-ios-guard-build/sandbox-check-20260702151755.json`.
- Murphy-style regression after the iOS sandbox guard build still passes for local virtual-payment reliability: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-after-ios-guard-build"`; evidence directory contains run `vplocal-20260702152634-758`. Cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- Murphy-style regression after the iOS sandbox guard build still passes for the main service-order paths: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-ios-guard-build"`. Cleanup removed 16 orders, 16 goods rows, and 4 refund rows.
- 2026-07-02 strengthened frontend observability for the sandbox guard by rejecting with both `errMsg` and `message`, and by attaching `errCode=-15011` for the iOS `env=1` branch. This keeps the real blocker visible even when upper layers prefer `errMsg`.
- Rebuilt the Windows `mp-weixin` output again after the observability patch; verified the compiled guard in `/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/unpackage/dist/dev/mp-weixin/core/payment/wechat.js` now emits `errMsg/message` plus `errCode=-15011`.
- Murphy-style regression after the observability patch still passes for local virtual-payment reliability: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-after-errcode-guard"`; evidence directory contains run `vplocal-20260702153258-252`. Cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- Murphy-style regression after the observability patch still passes for the main service-order paths: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-errcode-guard"`. Cleanup removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-02 added explicit operator-facing notes to `/opt/yoshop/虚拟支付/虚拟支付api.md` and `/opt/yoshop/虚拟支付/测试参考.md` so the current test contract is unambiguous: sandbox acceptance for this change is `Android + env=1 + 0.01`, while `iOS` must use `env=0` and `>= 1元`.
- Repeated attempts to satisfy the requested sub-agent adversarial review were made, but the external sub-agent service returned anti-probe/rate-limit errors (`403 Forbidden` and `429 Too Many Requests`) before producing a usable review. Main-agent manual review therefore remained the only available local verification path this round.
- 2026-07-02 discovered the earlier frontend `iOS + env=1` pre-check was too aggressive for the real DevTools + external-scan workflow. `wx.getSystemInfoSync()` in WeChat DevTools reports `brand=devtools`, `platform=devtools`, but still exposes an iPhone simulator `model/system`, so the client-side sandbox guard could falsely block Android QR-scan acceptance.
- Removed the client-side iOS sandbox pre-block from `yoshop2.0-uniapp/core/payment/wechat.js` and rebuilt the Windows `mp-weixin` output. This restores the intended contract: DevTools can emit the real virtual-payment QR flow, and the actual payment device / WeChat native layer decides whether the sandbox payment is supported.
- Fresh evidence after removing the pre-block confirms the old frontend iOS-only failure is no longer on the critical path: `/opt/yoshop/runtime/service-order-e2e/wechat-virtual-android-live-attempt-5.json` shows `beforeSubmit.systemInfo` still using the DevTools iPhone simulator while `observerState.marker` captured a real `wx.requestVirtualPayment` call with `signData/paySig/signature/outTradeNo`, and `cashierFlowState.submitResult.payment` remained `provider=virtual`, `platform=wechat_virtual`.
- 2026-07-02 fixed a second environment-drift source outside the PHP/backend codepath: the Windows-side mini-program mirror used by WeChat DevTools (`/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp`) had diverged from `/opt/yoshop/yoshop2.0-uniapp`. Its `config.js` had fallen back to `apiUrl: "http://127.0.0.1:8088/index.php?s=/api/"`, so DevTools could hit the wrong API origin even while the current workspace source still pointed at `https://wx.oiob.cn/index.php?s=/api/`.
- The same Windows-side mirror had an older `scripts/run-wechat-virtual-sandbox-e2e.cjs` that lacked the new automator-session cleanup/reopen logic. That meant a prior `cli auto` run could leave the operator's later manual payment attempt stuck in the `automator capture only` capture path even after the main workspace script had been fixed.
- Re-synced the full mini-program source to the Windows mirror and rebuilt `mp-weixin` using `node /opt/yoshop/yoshop2.0-uniapp/scripts/build-mp-weixin.cjs`; post-build verification confirmed both `/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/config.js` and `/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/unpackage/dist/dev/mp-weixin/config.js` now point to `https://wx.oiob.cn/index.php?s=/api/`, and the mirrored sandbox script now contains `shouldReopenManualDevtools`, `restoreManualDevtoolsSession()`, and `await miniProgram.close()`.
- After the Windows-mirror resync, Murphy-style local regression still passes: `php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-local-e2e-after-automator-cleanup"`; cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- After the Windows-mirror resync, the main service-order regression still passes: `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/runtime/service-order-e2e-after-automator-cleanup"`; cleanup removed 8 orders, 8 goods rows, and 4 refund rows.
- After the Windows-mirror resync, sandbox readiness still passes locally: `php think virtual-payment:sandbox-check --goods-id 10001 --evidence-dir "/opt/yoshop/runtime/virtual-payment-sandbox-check-after-automator-cleanup"` produced `/opt/yoshop/runtime/virtual-payment-sandbox-check-after-automator-cleanup/sandbox-check-20260702164036.json`.
- Re-opened WeChat DevTools in ordinary `cli open` mode against `D:\\Program\\0\\home\\0\\yoshop1\\yoshop2.0-uniapp\\unpackage\\dist\\dev\\mp-weixin` after the resync/rebuild so the next manual Android sandbox payment attempt uses the current build rather than the stale automator-tainted session.
- 2026-07-02 the requested adversarial sub-agent review finally completed and surfaced three harness risks: `run-wechat-virtual-sandbox-e2e.cjs` still defaulted `WECHAT_VIRTUAL_API_BASE` to `http://localhost`, it did not fail fast when the cashier never became ready, and `build-mp-weixin.cjs` could leave the Windows mirror source drifted after an `MP_WEIXIN_API_URL` override build.
- Fixed the harness environment split by making `run-wechat-virtual-sandbox-e2e.cjs` derive its default API root from `yoshop2.0-uniapp/config.js`, compute `expectedApiUrl`, and fail preflight if the workspace config, Windows mirror config, or compiled `mp-weixin/config.js` point at a different backend. This closes the same class of drift that had previously hidden behind `127.0.0.1:8088`.
- Fixed the harness false-confidence path by making `waitForCashierReady()` throw with the last cashier snapshot when readiness times out, instead of returning an ambiguous half-ready state that could still produce misleading evidence.
- Fixed the build drift risk by teaching `build-mp-weixin.cjs` to restore the Windows mirror `config.js` after a temporary `MP_WEIXIN_API_URL` override build, so the mirror source tree no longer stays permanently diverged from the main workspace after a successful compile.
- 2026-07-02 a separate production-adjacent blocker appeared during manual verification: PHP requests failed with `file_put_contents(...runtime/cache/...): Permission denied` because previous root-run CLI work had left `yoshop2.0/runtime` cache/log subtrees owned by `root:root` instead of `www-data:www-data`.
- Corrected the runtime ownership/permission drift with `chown -R www-data:www-data /opt/yoshop/yoshop2.0/runtime`, directory mode `775`, and file mode `664`; verified with `runuser -u www-data -- php -r 'file_put_contents(...)'` that the real PHP runtime user can write cache files again.
- After the runtime permission fix, sandbox readiness passes under the real PHP runtime user: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-permission-fix"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-permission-fix/sandbox-check-20260702170014.json`.
- After the runtime permission fix, Murphy-style local virtual-payment regression still passes under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-permission-fix"'`; cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- After the runtime permission fix, the main service-order regression still passes under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-permission-fix"'`; cleanup removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-02 further narrowed the sandbox automation path after adversarial review: a direct live probe confirmed `checkout/submit` can create the service order under `platform: MP-WEIXIN` (`{"status":200,"message":"订单创建成功","data":{"orderId":10238}}`), so `run-wechat-virtual-sandbox-e2e.cjs` no longer defaults its pre-payment order creation to `H5`.
- The sandbox automation script now derives its default API root from `yoshop2.0-uniapp/config.js`, computes an `expectedApiUrl`, and fails preflight if the workspace config, Windows mirror config, or compiled `mp-weixin/config.js` disagree. This turns the previously documented `127.0.0.1:8088` drift class into a hard error instead of an operator-memory footgun.
- The sandbox automation script now throws when the cashier never reaches a ready state, including the last cashier snapshot in the error. This removes the old behavior where a timeout could still flow into half-populated `realPayment` evidence.
- Rebuilt and re-synced the Windows mirror again after the harness tightening: `/mnt/d/Program/0/home/0/yoshop1/yoshop2.0-uniapp/scripts/run-wechat-virtual-sandbox-e2e.cjs` now contains `orderPlatform='MP-WEIXIN'`, `expectedApiUrl`, `ensureApiAlignment()`, and the strict `cashier not ready` timeout.
- 2026-07-03 reran sandbox readiness under the real PHP runtime user: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-current"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-current/sandbox-check-20260703113452.json`.
- That readiness evidence is now fully green on config, goods mapping, stock, MP-WEIXIN OAuth/session data, notify route, and recent real `wechat_virtual` trade creation. The latest audited non-local trades are `trade_id=10106,10095,10094`; all three remain `trade_state=10` and `notify_times=0`, so no genuine WeChat sandbox paid callback/query evidence exists yet.
- 2026-07-03 reran the local virtual-payment reliability fixture under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-current"'` passed with evidence `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-current/check-20260703113452.json`.
- That fixture covers the core `PaymentTrade` / `OrderPaySuccess` convergence logic behind 11.8 and 11.9: duplicate notify, query fallback, replayed `out_trade_no`, and `provide_goods` idempotency; cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- 2026-07-03 reran the Murphy-style main service-order regression under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-current"'` passed with evidence `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-current/check-20260703113558.json` and `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-current/real-order-20260703113558.json`; cleanup removed 8 orders, 8 goods rows, and 4 refund rows.
- 2026-07-03 audited the latest real sandbox trade directly: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --out-trade-no 331118899646474824 --expect-paid --expect-pending-contact --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-real-pending"'` failed as expected with evidence `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-real-pending/sandbox-check-20260703114223.json`.
- That direct trade audit proves the remaining gap is specifically the missing WeChat paid result on a real virtual-payment order: `trade.platform` and `trade.product_id` pass, but `trade.paid`, `order.paid`, `order.trade_link`, and `order.pending_contact.pay_status` all fail on the same real `out_trade_no`.
- 2026-07-03 adversarial review found the earlier local fixture still missed the loser-trade duplicate-refund guard branch. Strengthened `php think virtual-payment:local-e2e check` to simulate late callbacks on the second trade after `virtual_refund.status=processing`, then reran it under `www-data`: `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-duplicate-refund-guard/check-20260703115312.json`.
- The strengthened fixture now explicitly proves `late duplicate-payment callbacks do not reissue virtual refunds while refund is already processing`, in addition to the earlier duplicate-notify/query-fallback/replay/idempotent-provide-goods checks; cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- 2026-07-03 a fresh manual Android retry created another real virtual-payment trade `trade_id=10111`, `out_trade_no=331399720368045620`, `platform=wechat_virtual`, `product_id=vip0`, which confirms the backend still routes the cashier into the virtual-payment path rather than ordinary WeChat pay.
- Auditing that fresh trade still fails only on paid-state convergence: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --out-trade-no 331399720368045620 --expect-paid --expect-pending-contact --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10111"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10111/sandbox-check-20260703120009.json`; `trade.platform` and `trade.product_id` pass, while `trade.paid`, `order.paid`, `order.trade_link`, and `order.pending_contact.pay_status` still fail.
- 2026-07-03 narrowed the DevTools-side blocker further by removing a frontend pre-call `wx.requestVirtualPayment` source-string probe that could falsely reject before QR emission if the DevTools runtime exposed internal `automator capture only` text in the function source. The cashier now only classifies `automator capture only` after a real fail callback, persists the last virtual-payment failure payload in local storage key `virtualPaymentLastFail`, and prefers the normalized `message` field over raw `errMsg` in the cashier popup. Rebuilt and re-synced `mp-weixin` output with `node /opt/yoshop/yoshop2.0-uniapp/scripts/build-mp-weixin.cjs`; current source, Windows mirror, and compiled `config.js` all still point to `https://wx.oiob.cn/index.php?s=/api/`.
- 2026-07-03 a second adversarial review on the frontend diagnostics found a real cashier-state regression: failed virtual-payment attempts could leave stale `tempUnifyData_*` and a global `virtualPaymentLastFail` behind, causing later cashier visits to auto-query old `out_trade_no` values and mix failure hints across retries of the same order.
- Fixed that state leak by consuming `tempUnifyData_*` on read again, clearing temporary recovery state on native virtual-payment fail, and namespacing `virtualPaymentLastFail` per `orderKey` instead of a single global slot. The cashier success path now also clears the order-scoped failure record, while failure popups append `错误码` and `交易号` from the current order-scoped virtual-payment failure snapshot for manual triage. Rebuilt and re-synced `mp-weixin` output again with `node /opt/yoshop/yoshop2.0-uniapp/scripts/build-mp-weixin.cjs`.
- Murphy-style regression after the frontend state-leak fix still passes for the local virtual-payment convergence fixture under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:local-e2e check --goods-id 10001 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-state-leak-fix"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-state-leak-fix/check-20260703120911.json`; cleanup removed 3 orders, 4 trades, 3 goods rows, and 0 refund rows.
- Murphy-style regression after the frontend state-leak fix still passes for the main service-order paths under `www-data`: `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10001 --service-goods-sku-id 0 --min-balance 200 --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-state-leak-fix"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-state-leak-fix/check-20260703120912.json` and `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-state-leak-fix/real-order-20260703120912.json`; cleanup removed 7 orders, 7 goods rows, and 4 refund rows.
- 2026-07-03 extended `php think virtual-payment:sandbox-check` with optional `--probe-remote-query`, which calls WeChat `xpay/query_order` for an audited real trade and stores the raw remote response in evidence without mutating business state. This closes a previous observability gap where readiness was fully green locally but the workspace still could not distinguish “微信远端未建单 / 未支付 / 已支付 / 非支付终态”.
- Remote probes against two separate real Android sandbox attempts now show a stable upstream pattern. For `out_trade_no=331399720368045620`, `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --out-trade-no 331399720368045620 --expect-paid --expect-pending-contact --probe-remote-query --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10111-remote"'` produced `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10111-remote/sandbox-check-20260703121351.json`; WeChat returned `errcode=0` plus `order.status=6`, while the local trade/order still remained unpaid.
- A second fresh trade created after the test user's MP-WEIXIN oauth row had just been refreshed still converged to the same remote terminal state. `out_trade_no=331404686426410604` (trade `10116`) was created at `2026-07-03 12:03:54 CST`, the same second that `yoshop_user_oauth.update_time` for user `10004` moved forward, yet `runuser -u www-data -- bash -lc 'cd /opt/yoshop/yoshop2.0 && php think virtual-payment:sandbox-check --goods-id 10001 --out-trade-no 331404686426410604 --probe-remote-query --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10116-remote"'` still returned `errcode=0` and `order.status=6` in `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10116-remote/sandbox-check-20260703121544.json`. This weakens the earlier stale-`session_key` hypothesis: a freshly refreshed session still lands in the same non-paid upstream status.
- 2026-07-03 added `php think virtual-payment:oauth-audit` as a narrow maintenance/diagnostic command for duplicate MP-WEIXIN identity bindings, and fixed `app/common/model/UserOauth::getOauth()` to honor `is_delete=0` so soft-deleted oauth rows cannot continue to participate in payment/refund/runtime flows.
- Dry-run evidence from `cd /opt/yoshop/yoshop2.0 && php think virtual-payment:oauth-audit` is stored at `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-oauth-audit/audit-20260703122821.json`. It shows the effective duplicate remains exactly two active bindings on the same openid: `user_id=10003` and `user_id=10004`; older rows `10001/10002` are already logically deleted. This lines up with the latest readiness failure `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-oauth-audit-command/sandbox-check-20260703122840.json`, which now fails only on `test_user.oauth_unique`.
- The requested adversarial sub-agent review then tightened that maintenance path further before any destructive cleanup was executed: `virtual-payment:oauth-audit` no longer defaults to the live test mobile, `--apply` now requires `--confirm-openid`, cleanup scope follows the same global `oauth_type + oauth_id` uniqueness rule used by checkout, and the command refuses to keep a binding that lacks `session_key` or belongs to a deleted user. The hardened dry-run evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-oauth-audit/audit-20260703123429.json`.
- After hardening the audit path, Murphy-style regressions still pass under the real PHP runtime user: `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-oauth-audit-hardening/check-20260703123442.json` and `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-oauth-audit-hardening/check-20260703123443.json`. Sandbox readiness still fails only on duplicate active bindings, with evidence `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-oauth-audit-hardening/sandbox-check-20260703123442.json`.
- 2026-07-03 then executed the scoped oauth cleanup for the duplicated MP-WEIXIN openid, keeping only `row_id=10004 / user_id=10004`: `cd /opt/yoshop/yoshop2.0 && php think virtual-payment:oauth-audit --user-mobile=19900000000 --apply --keep-oauth-row-id=10004 --confirm-openid=o1Wtm3eQK7SFsb_RVCKUToFS3ENo`. The apply evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-oauth-audit/audit-20260703123721.json`, and the post-apply verification evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-oauth-audit/audit-20260703123732.json`.
- Post-cleanup readiness is now fully green again under the real PHP runtime user: `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-oauth-cleanup/sandbox-check-20260703123748.json` passes `test_user.oauth_unique`, proving the previous identity-ambiguity blocker has been removed. Murphy-style regressions also still pass after the cleanup: `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-oauth-cleanup/check-20260703123748.json` and `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-oauth-cleanup/check-20260703123749.json`.
- 2026-07-03 also verified the post-cleanup backend path can immediately create a fresh real `MP-WEIXIN` virtual-payment order for `user_id=10004` without falling back to ordinary pay. A direct API probe using the debug identity (`token -> userId=10004`) created `order_id=10317 / order_no=331414357904212732`, then `cashier/orderPay` returned `provider=virtual`, `platform=wechat_virtual`, `out_trade_no=331414359873826236`, `product_id=vip0`, `env=1`, plus the expected `signData/paySig/signature`.
- Auditing that backend-only fresh trade clarified a first-principles boundary in the WeChat flow: before the client actually invokes `wx.requestVirtualPayment`, WeChat remote query does **not** know the order yet. `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10317-remote/sandbox-check-20260703124249.json` shows local trade `10141` is recorded correctly, but remote `xpay/query_order` returns `errcode=268490002` (`数据不存在`). This is consistent with “服务端 `orderPay` 只生成客户端拉起所需签名参数，真正的微信侧建单发生在客户端实际调用 `wx.requestVirtualPayment` 之后”.
- 2026-07-03 added `php think virtual-payment:watch-live` to reduce the next manual Android retry from a human-log hunt into a single evidence artifact. The command watches for a new real `wechat_virtual` trade after a given `since-trade-id`, captures the new trade/order/refund payloads, and can immediately probe WeChat `xpay/query_order` for that new trade. It was then extended to keep observing a short settlement window after capture, polling local order/trade convergence and remote query state so a real paid attempt can self-report whether it actually reached `paid` or remained stuck. Self-check evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-selfcheck/watch-20260703124612.json`; the post-settlement self-check is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-settle-selfcheck/watch-20260703124857.json`, which correctly classified trade `10141` as captured-but-unpaid and remote-query-unconfirmed.
- A follow-up adversarial review then tightened `virtual-payment:watch-live` into an actual acceptance observer instead of a loose debugger. It now requires `--goods-id`, auto-baselines against the latest matching trade when `--since-trade-id` is omitted, refuses to treat historical trades as “live” captures, and returns non-zero unless a newly captured trade actually converges to the paid service-order state (plus remote paid status when `--probe-remote-query` is enabled). It also switched settlement observation from a single fixed `trade_id` view to an order-level multi-trade view so repeated-attempt / late-callback convergence is judged by the order’s final linked trade rather than the first newly seen retry row. Hardened self-check evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-newbaseline-selfcheck/watch-20260703125511.json`, which correctly times out instead of reusing an old trade when no new attempt happens.
- A final Murphy-style pass then polished the acceptance observer’s error/evidence path: `--goods-id` validation now still writes an evidence file via the normal catch path, trade payloads preserve both raw and decoded `payload_snapshot`, and evidence filenames include a finer-grained suffix to reduce rapid-rerun collisions. Final self-check evidence is `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-final-selfcheck/watch-20260703125631-912849.json`.
- The same Murphy-style evidence-hardening pass was also applied back to the shared acceptance commands. `virtual-payment:sandbox-check` and `virtual-payment:local-e2e` now fail loudly if evidence directories/files cannot be created or written, and `sandbox-check` evidence filenames now also include a finer-grained suffix. Post-hardening regressions still pass under the real PHP runtime user: `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-check-after-evidence-hardening/sandbox-check-20260703125842-221422.json`, `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-local-e2e-after-evidence-hardening/check-20260703125842.json`, and `/opt/yoshop/yoshop2.0/runtime/codex-evidence/service-order-e2e-after-evidence-hardening/check-20260703125843.json`.
- As of `2026-07-03 12:23:52 CST`, the database still shows no new real `wechat_virtual` trade after `trade_id=10116 / out_trade_no=331404686426410604 / 2026-07-03 12:03:54 CST`. So the latest operator-observed DevTools popup `requestVirtualPayment:fail automator capture only` did not correspond to a fresh backend virtual-payment attempt, which further supports treating it as DevTools-session noise rather than new payment-state evidence.
- As of 2026-07-03, `11.1` is the only remaining open validation item. The code path, runtime config, DevTools build alignment, and backend convergence checks are all ready, but the workspace still lacks a real WeChat sandbox `0.01` paid trade with official paid callback or paid query result.
- 2026-07-03 aligned the local diagnostics with the latest upstream interpretation: `virtual-payment:sandbox-check` and `virtual-payment:watch-live` now classify `xpay/query_order status=6` as `remote_order_closed` instead of lumping it into a generic not-paid bucket. The Windows-side manual DevTools reset helper now also kills `wechatwebdevtools.exe`, the main `run-wechat-virtual-sandbox-e2e.cjs` cleanup path was hardened to kill the same auxiliary DevTools process before and after `cli auto`, a Linux-side wrapper `scripts/reset-wechat-devtools-manual.sh` can now invoke the Windows cleanup helper directly via `cmd.exe`, and the manual helper reminds the operator to retry with an uploaded experience-version QR if the live preview path still reproduces the same failure.
- 2026-07-03 converted more of the report's manual advice into executable guardrails: the WSL wrapper `scripts/reset-wechat-devtools-manual.sh` now switches into `/mnt/c` before invoking `cmd.exe` so it can call the Windows helper directly instead of tripping over the earlier quoted-path bug; the DevTools reset helper and the main automator script now both accept `WECHAT_DEVTOOLS_PORT` for environments where the default IDE port cannot be initialized cleanly; `scripts/upload-wechat-experience.cmd` plus `scripts/upload-wechat-experience.sh` now provide a repeatable experience-build upload path for the recommended preview-vs-experience split test; and the mini-program payment client now records runtime context (`envVersion`, platform, requestVirtualPayment source snippet) and short-circuits obvious automator-mock / sandbox-on-release cases before the user retries payment.
- A follow-up Windows-side probe then showed a more specific DevTools CLI behavior: `cli.bat --port 3901 ...` does parse the override argument, but it still prints `#initialize-error: Error: listen EACCES: permission denied 127.0.0.1:3799` and exits with code `0`. The reset/upload helpers now treat `#initialize-error` as a real failure instead of trusting the misleading zero exit code; the manual-reset path falls back to launching `wechatdevtools.exe` directly, while the experience-upload helper now aborts loudly and tells the operator to reopen DevTools manually before retrying upload.
- 2026-07-03 a clean experience-build retry removed the last serious "DevTools live preview only" ambiguity. `scripts/reset-wechat-devtools-manual.sh` reopened the Windows project cleanly at IDE port `18509`, `scripts/upload-wechat-experience.sh` then successfully uploaded experience version `vp-sandbox-2026.0703.2058`, and `php think virtual-payment:watch-live --goods-id=10001 --timeout-seconds=240 --settle-seconds=75 --probe-remote-query --evidence-dir "/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-experience-retry"` captured a fresh Android Mini Program trade `10173 / out_trade_no=331539720188296576 / order_id=10361`.
- The latest attempt trace proves this was no longer a DevTools preview request: `/opt/yoshop/yoshop2.0/runtime/api/codex-evidence/virtual-payment-attempt-trace/20260703.jsonl` shows `referer=https://servicewechat.com/wxbf57e6f2ba72b712/0/page-frame.html` plus Android `MiniProgramEnv/android` user-agent when `cashier/orderPay` built `provider=virtual`, `platform=wechat_virtual`, `product_id=vip0`, `env=1`, and `out_trade_no=331539720188296576`.
- The same experience-build attempt still failed to converge to paid state upstream. Watch evidence `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-watch-live-experience-retry/watch-20260703210147-070922.json` records WeChat remote-query status transitioning `0 -> 1 -> 6` within seconds for the fresh order before local notify/query convergence ever marked it paid. Follow-up audit `/opt/yoshop/yoshop2.0/runtime/codex-evidence/virtual-payment-sandbox-audit-trade-10173/sandbox-check-20260703210210-301427.json` still fails `trade.paid`, `order.paid`, `order.trade_link`, and `order.pending_contact.pay_status`, while `remote_query.status=6` confirms the upstream order was closed before payment success.
- Therefore, as of 2026-07-03 21:02 CST, the remaining blocker is no longer explainable by stale DevTools preview sessions alone. The local system now has direct evidence that `Android + experience build + real wechat_virtual trade creation` still lands in WeChat upstream close-order behavior, so the next root-cause candidates are narrowed to external sandbox/account/product governance (for example test-account eligibility, sandbox product publication/whitelist state, or other WeChat-side xpay controls) rather than the local cashier route or signing implementation.
