# 2026-07-14 iOS App Store 服务订单退款优化改动

## 一、完成内容

本轮严格限定在 **iOS App Store 服务订单退款流程**，没有加入积分、结算、累计消费、优惠券、库存、独立告警表、监控守护、通用队列或退款取消模块。

### 1. 状态与数据模型

- 新增 iOS 退款风险状态：`NONE(0) -> LOCKED(10) -> REFUNDED(20)`，只允许单向推进。
- 新增风险来源：`LOCAL_APPLY`、`APPLE_INQUIRY`、`REFUND_NOTIFY_RECOVERY`。
- 订单增加风险状态、首次来源、首次时间三个字段。
- 新增 Apple 问询历史表，独立保存每次认证问询、服务/审核快照、响应决定、证据和耗时。
- API/UI 投影不暴露原始 payload、指纹或迁移键。

### 2. 本地退款申请

- iOS 服务退款申请创建退款单与 `NONE -> LOCKED` 在同一事务完成。
- 未开始服务：自动 `REVIEWED`，提示用户前往 App Store。
- 已开始未完成：创建 `WAIT`，等待商家审核。
- 已完成服务：后端拒绝，且不创建退款单。
- `LOCKED/REFUNDED` 或已有 iOS 服务退款记录时拒绝重复申请。
- iOS 始终 `can_cancel=false`，未新增退款取消路由。

### 3. Apple 问询与商家审核

- 每次认证问询都读取当时最新商家审核状态，相同 payload 不固化第一次决定。
- 决策矩阵：
  - 未开始且自动同意：建议退款；
  - 已开始 + WAIT/REJECTED：建议拒绝；
  - 已开始 + REVIEWED：建议退款；
  - 已完成/未知：建议拒绝。
- 每次问询独立留痕，但退款单、风险冻结等副作用保持幂等。
- final trade、order/store/user/out-trade、支付状态和交易状态全部在锁内校验。
- 绑定失败使用 `order_id=0` 做安全审计，不污染候选业务订单时间线。
- 异常统一 fail-closed；风险锁写入失败会整体回滚，不能返回建议退款。
- 商家同意不调用 iOS 开发者主动退款；商家驳回不解除冻结。

### 4. 服务与 provide_goods 并发保护

- `startService()`、`completeService()` 在事务内按订单优先加锁并重新检查风险、退款和服务状态。
- 增加真实双连接测试，覆盖：
  - apply-first / start-second；
  - start-first / apply-second；
  - inquiry / complete；
  - merchant audit / inquiry；
  - inquiry / provide_goods dispatch claim。
- provide_goods 使用 `order -> trade` 原子风险检查并复用现有 dispatch claim：
  - 风险冻结先提交，不能建立新发送权；
  - 发送权先提交，后续问询线性化在已有发送操作之后。
- 没有新增 worker、lease、claim 表或独立调度系统。

### 5. 退款成功收口

- 只接受严格成功码：整数 `0` 或字符串 `"0"`；`FAIL`、`SUCCESS`、`00`、空值、布尔值等全部 fail-closed。
- Notify、退款服务和历史回填共用同一严格判断。
- 可信成功通知在订单锁内收口 order/trade/risk/refund。
- 保留服务开始/完成历史和商家审核历史；不调用通用积分/优惠券/库存冲正流程。
- 无退款单时补建；已有唯一退款单时完成；重复通知幂等。
- 非最终交易保持安全冲突结果，不污染正常订单。

### 6. 历史回填

- 新增命令：

```bash
php think virtual-payment:ios-refund-risk-backfill
php think virtual-payment:ios-refund-risk-backfill --apply
```

- 只认可真实本地服务退款、认证问询快照、可信退款成功通知。
- 不使用 `ios_refund_required` 单独作为问询证据。
- 写入返回 false 时抛错回滚，不能虚报 `changed/errors=0`。
- 本地已完成 apply，最终 dry-run：`changed=0`、`errors=0`。

### 7. 小程序与商家后台

- 小程序按 WAIT、REVIEWED、REJECTED、等待 App Store、REFUNDED 展示后端权威文案。
- iOS 隐藏上传凭证和取消入口，保留“退款”“提交退款申请”。
- 订单详情和退款申请页 API 失败时显示安全错误与重试，不永久 loading、不展示可误操作表单。
- 商家订单/售后列表在既有状态区域展示 App Store 标签，不新增表格列、菜单或消息中心。
- 商家详情展示不可忽略的风险警告、冻结原因和问询时间线。
- 操作按钮由后端 action flags 控制。

### 8. 唯一一次代码 Review 缺陷闭环

已完成一次限定范围双代理对抗审查；另有唯一代码 Review 子代理：`019f5d6f-a9dd-7fa3-820b-4ea7577bf267`。

- 初始发现：P0=3、P1=5、P2=1。
- 3 个 P0、5 个 P1 已全部验证、修复并复测；本轮对抗审查新增的 Android/非 Apple 证据缺口已通过独立合同补齐。
- 当前开放：**P0=0、P1=0**。
- 剩余 P2=1：订单列表 refund/trade projection accessor 存在性能型 N+1 债务；不影响本轮 iOS 退款正确性，为避免扩大范围未在本轮重构。

完整记录：

- `.trellis/tasks/07-13-ios-refund-review-service-lock/code-review.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/verification.md`

## 二、主要修改文件

### Trellis / 规范

- `.trellis/tasks/07-13-ios-refund-review-service-lock/prd.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/design.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/implement.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/risk-test-matrix.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/plan-review.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/code-review.md`
- `.trellis/tasks/07-13-ios-refund-review-service-lock/verification.md`
- `.trellis/spec/backend/virtual-payment-contracts.md`
- `.trellis/spec/frontend/service-order-contracts.md`

### 后端 / 数据库

- `yoshop2.0/app/common/enum/order/iosRefund/RiskStatus.php`
- `yoshop2.0/app/common/enum/order/iosRefund/RiskSource.php`
- `yoshop2.0/app/common/model/PaymentIosRefundInquiry.php`
- `yoshop2.0/app/common/service/order/IosRefundRisk.php`
- `yoshop2.0/app/common/service/order/Refund.php`
- `yoshop2.0/app/common/command/VirtualPaymentIosRefundRiskBackfill.php`
- `yoshop2.0/app/api/service/Notify.php`
- `yoshop2.0/app/api/model/Order.php`
- `yoshop2.0/app/api/model/OrderRefund.php`
- `yoshop2.0/app/store/model/Order.php`
- `yoshop2.0/app/store/model/OrderRefund.php`
- `yoshop2.0/app/timer/service/Order.php`
- `yoshop2.0/config/console.php`
- `yoshop2.0/public/install/data/install.sql`
- `yoshop2.0/数据库修改记录/v2.1.3_ios_refund_risk_lock.sql`

### 自动测试

- `yoshop2.0/scripts/tests/ios-refund-review-lock-contract.php`
- `yoshop2.0/scripts/tests/ios-refund-concurrency-contract.php`
- `yoshop2.0/scripts/tests/ios-refund-state-matrix-contract.php`（48 个服务×审核×风险组合，Mock Apple 回调）
- `yoshop2.0/scripts/tests/non-ios-service-refund-regression-contract.php`（非 Apple apply/reject/reapply 回归）
- `yoshop2.0-store/tests/unit/order/Detail.spec.js`
- `yoshop2.0-store/tests/unit/order/Index.spec.js`
- `yoshop2.0-store/tests/unit/order/refund/Detail.spec.js`

### 小程序

- `yoshop2.0-uniapp/components/refund/IosAppleRefundGuide.vue`
- `yoshop2.0-uniapp/pages/order/detail.vue`
- `yoshop2.0-uniapp/pages/refund/apply.vue`
- `yoshop2.0-uniapp/pages/refund/detail.vue`

### 商家后台

- `yoshop2.0-store/src/views/order/Index.vue`
- `yoshop2.0-store/src/views/order/Detail.vue`
- `yoshop2.0-store/src/views/order/refund/Index.vue`
- `yoshop2.0-store/src/views/order/refund/Detail.vue`

> 工作树同时存在其他任务的未提交改动；本轮没有 reset、clean 或批量覆盖，也没有把其他任务文件归入本任务提交。

## 三、测试结果

| 检查 | 结果 |
|---|---|
| PHP 相关文件语法检查 | 20/20 PASS |
| `ios-payment-channel-contract.php` | PASS |
| `ios-refund-review-lock-contract.php` | PASS |
| `ios-refund-concurrency-contract.php` | PASS，真实双连接 |
| `ios-refund-state-matrix-contract.php` | PASS，48/48 组合，1347 条断言 |
| `non-ios-service-refund-regression-contract.php` | PASS，非 Apple apply/reject/reapply 与风险隔离 |
| 严格 RetCode / 绑定失败 / 重复通知回归 | PASS |
| backfill 最终 dry-run | `changed=0`, `errors=0` |
| 商家 Jest | 3 suites / 20 tests PASS |
| 商家 focused lint | PASS，无错误 |
| 商家生产构建 | PASS；仅既有 CSS 顺序 warning |
| 小程序 H5 构建 | PASS；仅既有 Sass deprecation warning |
| 小程序 mp-weixin 编译 | PASS，HBuilderX 5.15 dev artifact |
| `git diff --check` | PASS |
| Trellis validate | PASS |
| `nginx -t` | PASS |
| Mock Apple 三轴矩阵 | 48/48 组合、1347 条断言 PASS |
| 45 项墨菲矩阵 | 已逐项形成自动/代码检查/外部门禁证据 |
| 范围回归扫描 | PASS；未新增积分/结算/库存/告警/worker/取消路由 |

## 四、上线准备状态

### 本轮新增发布前证据

- 数据库 schema/index 审计通过：风险字段、24 列 inquiry 表和 5 组索引均存在。
- backfill dry-run 保持 `changed=0`、`errors=0`，已知冲突 1 条安全跳过。
- 商家 dist 与临时 staging 完成 `rsync --delete` 模拟，二次 checksum dry-run 为 0 差异。
- staging bundle 已验证 `App Store退款`、`服务已冻结`、`ios_refund_inquiry_timeline` marker。
- 当前实际 HTTP 仍指向旧 bundle，未把 staging 模拟误报成线上发布。


### 已完成

- 本地 additive migration 已应用。
- 本地 backfill 已执行并验证幂等。
- 后端守卫、前端源码和构建产物均已验证。
- Nginx 配置语法通过。
- 发布产物门禁能识别未同步 bundle。

### 尚未执行发布

当前商家构建产物与实际 Nginx 目录不一致，门禁正确拦截：

```text
dist:   order.5035168f.js / order.f5230d9d.css
public: order.721c9fad.js / order.fbc3d323.css
```

本轮没有擅自执行：

- `dist/ -> yoshop2.0/public/store/` 同步；
- PHP-FPM/Nginx reload；
- 公网 bundle 切换；
- 小程序体验版上传/发布。

建议正式发布顺序：

1. 备份数据库并确认增量 SQL；
2. 上线 schema；
3. 上线后端风险守卫并 reload PHP-FPM/opcache；
4. dry-run → 人工核对 → apply → 再 dry-run；
5. 同步商家 `dist/` 和小程序体验版；
6. `nginx -t`、reload；
7. 校验实际 HTTP bundle marker；
8. 用真实 iOS 样本验证申请、问询、审核、退款成功和冻结接口。

## 五、遗留风险

本轮 mp-weixin 首次构建因 HBuilderX 存在同名旧 WSL 项目注册而失败；关闭旧注册、重新导入 Windows 镜像后，单独重跑 `npm run build:mp-weixin` 已通过。

1. **外部真实链路**：仍需 Apple/微信真实签名问询和退款成功通知验收；本地只能验证协议适配和业务状态机。
2. **小程序体验版**：mp-weixin 已编译，但尚未上传微信开发者工具并做真机验收。
3. **商家视觉验收**：1280/1366px、100% 缩放及真实权限数据需要登录态人工检查。
4. **发布目录未同步**：这是刻意保留的上线门禁，不是构建失败。
5. **历史冲突样本**：trade `10333` 的订单最终交易为 `0`，backfill 安全跳过并计为 1 个 conflict，需要人工核对，不会误冻结。
6. **P2 性能债**：部分订单列表 refund/trade accessor 仍可能产生 N+1；不影响退款正确性，建议后续独立性能任务处理。
7. **混合工作树**：当前有 55 项未提交状态，包含其他 Trellis 任务；提交/发布时必须按文件清单精确选择，禁止批量提交或清理。

## 六、结论

- 开发、自测、唯一一次代码 Review、有效缺陷修复、缺陷复测、45 项风险证据和 48 个状态组合矩阵和上线准备已完成。
- 当前代码 Review 开放缺陷：`P0=0`、`P1=0`。
- **尚未发布到实际商家目录或小程序体验版**；发布需要明确授权和真实环境验收。
