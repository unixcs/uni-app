# 微信虚拟支付上线前完整功能验收报告（2026-07-07）

## 先说结论

一句话：**主链路已经比之前稳很多了，虚拟支付“能付、能退、能收口”这三件事现在基本打通了。**

这次又补了一轮后，当前更像是：
- 大漏口已经补上了
- 小漏口又扫出 1 个，也顺手修了
- 还需要你最后配合做一轮**真机手动回归**，再决定是否上线/提测

我现在给的建议是：**可以继续提测，但正式上线前，最好把最后那几条真机手动项再走一遍。**

---

## 0. grill-with-docs / grill-me 当前状态

这两个插件我已经挂到**当前项目目录**下了，不是全局装：

- `/opt/yoshop/.codex/skills/grill-me`
- `/opt/yoshop/.codex/skills/grill-with-docs`

说明：
- 它们现在已经在**项目本地**可见
- 但这次会话的官方技能列表里，还是没自动把它们挂出来
- 所以我这次是**按它的思路在做**：边排查，边留文档，边做碰撞审查

---

## 1. 这次主要改了什么

### 1）先把“钱到了，但本地没跟上”的大漏口补掉

补了几类最容易出事的地方：

- 定时任务现在会**先查虚拟支付状态，再决定关单**
  - 避免“微信那边其实付了，本地先把单关掉”
- 支付待收口时，前端和接口会明确给出：
  - `支付确认中`
  - `支付成功待入账`
- 退款通知现在不会“假装收到了就算完事”
  - 如果本地还没真正收口，会让它重试
- 退款查单、退款完成、trade 状态更新，补成同一条闭环
- 用户端和后台端，把“退款中的单”从普通服务流程里摘出来
  - 不再混进“待联系 / 服务中”这些桶里
- 后台详情页改成优先看**当前有效退款单**
  - 避免页面展示 A 退款，按钮却在操作 B 退款
- 新支付单会额外留付款时的 openid 证据
  - 避免后面用户身份漂了，查单/退款补偿找不到人
- 下单时补了金额一致性校验
  - 避免平台价格、购买数量、订单金额三边对不上

### 2）又修了一个这轮自动回归扫出来的小 bug

自动验收时扫出一个小口子：

- 退款被拒绝时
- H5 那边期望文案是 `退款已拒绝`
- 实际接口给的是 `已拒绝退款`

本质上不是功能坏了，是**前后文案没统一**。

我已经顺手修掉了，统一成：
- `退款已拒绝`

---

## 2. 自动化测试文件清单

这次主要用了项目里现成的这几组脚本/命令：

1. `yoshop2.0/app/common/command/VirtualPaymentLocalE2e.php`
   - 本地模拟虚拟支付核心链路
2. `yoshop2.0/app/common/command/VirtualPaymentSandboxCheck.php`
   - 沙箱配置、订单、trade、远端查单证据检查
3. `yoshop2.0/app/common/command/ServiceOrderE2eAutomation.php`
   - 服务单主流程验收
4. `yoshop2.0/app/common/command/InspectServiceOrders.php`
   - 巡检服务单脏数据/混乱状态
5. `yoshop2.0/app/common/command/VirtualPaymentWatchLive.php`
   - 用来看真实新单的实时证据（这轮没强依赖它收结论）

---

## 3. 已执行测试结果

## A. 本地虚拟支付链路回归

执行命令：
- `php think virtual-payment:local-e2e check`

结果：**通过**

覆盖到的点：
- 单订单重复支付通知，不会把订单打乱
- 重复通知不会多生成退款单
- 主动查单拿到 paid 状态后，订单能收口成已支付
- 同一订单多次支付尝试，历史 out_trade_no 仍可追溯
- 重复支付输家退款，晚到回调不会重复发起退款
- provide_goods 补偿是幂等的，不会重复乱发

本轮运行结果：
- `local virtual-payment checks ok: vplocal-20260707192246-530`
- 证据文件：`/opt/yoshop/runtime/virtual-payment-local-e2e/check-20260707192246.json`

---

## B. 服务单主流程验收

第一次执行：
- `php think service-order:e2e check --mode=real --check=all`

第一次结果：**失败 1 条**
- 失败点：退款拒绝文案不一致
- 期望：`退款已拒绝`
- 实际：`已拒绝退款`

我已修复后再次执行。

第二次执行结果：**全部通过**

通过项：
- `h5 real-order checks ok`
- `backend real-order checks ok`
- `mp-weixin real-order checks ok`

说明：
- 用户端
- 后台端
- 小程序端投影

这一轮至少说明：**主流程状态和按钮口径，已经比之前统一很多了。**

---

## C. 服务单脏数据巡检

执行命令：
- `php think inspect:service-orders --limit=20`

结果：**通过**

关键输出：
- `physical_clean: 0`
- `service_clean: 40`
- `mixed_dirty: 0`
- `service_suspicious: 0`

这说明当前服务单视角下，没再扫出明显的“订单类型混乱 / 服务状态脏桶”问题。

---

## D. 沙箱订单抽查

### 抽查 1：未支付样本单 10418

执行命令：
- `php think virtual-payment:sandbox-check --out-trade-no=332930366979909814 --probe-remote-query`

结果：**通过**

结论：
- 远端状态返回 `status=6`
- 这类单本质上不是“其实付了但本地没跟上”
- 更像是：**远端已关单/未成功支付，本地保持未支付是对的**

### 抽查 2：已退款样本单 10423

执行命令：
- `php think virtual-payment:sandbox-check --out-trade-no=332930569128690632 --probe-remote-query`

结果：**通过**

结论：
- 远端状态返回 `status=5`
- 对应本地退款单已收口完成
- 不再是“钱退了，但前端还卡退款处理中”

---

## E. SQL 异常口扫表

我又额外跑了一轮脏数据排查，重点盯这些：

- 已支付但没绑 trade
- 已支付但绑定的 trade 不是成功/退款态
- 未支付但其实已经有成功 trade
- 未支付但快照里已经出现 paid-like 状态
- 退款完成但 trade 还没进退款态
- 还有没有活动退款单没收口

本轮结果全部是 **0**：

- `paid_but_no_trade = 0`
- `paid_but_bound_trade_not_success = 0`
- `pending_but_has_success_trade = 0`
- `pending_but_paid_like_snapshot = 0`
- `refund_completed_but_trade_not_refund = 0`
- `active_refund_orders = 0`

这项很重要。

它说明：**当前数据库里，至少没再扫出一批“钱到了/钱退了，但本地主状态没跟上”的现成脏单。**

---

## 4. 样本问题复核结果

## 样本 1：订单 332930560595366336（order_id=10423）

你反馈的问题：
- 钱已经退了
- 小程序前端还显示 `退款处理中`

当前复核结果：**已收口**

现在库里状态是：
- 订单：已支付 + 已取消
- 退款单：已完成
- trade：已退款
- 虚拟退款快照：`completed`

结论：
- 这个问题已经不是“待修复”了
- 现在属于**已修复并已落库**

---

## 样本 2：订单 332872263196571540（order_id=10417）

当前复核结果：**已收口**

现在库里状态是：
- 订单：已支付 + 已取消
- 退款单：已完成
- trade：已退款

结论：
- 这类“退款完成但本地还没跟上”的同类问题，也已经被一起收回去了

---

## 样本 3：订单 332874224676467417（order_id=10418）

这个单之前最容易让人误会成：
- 用户像是付过
- 但本地没认账

这次复核后，我更倾向于把它定性成：
- 同一业务单有多次虚拟支付尝试
- 本地都没有认账成功
- 远端抽查返回 `status=6`
- 所以它更像是**没付成/已关闭**，不是“钱到了但本地没跟上”

结论：
- 这个样本当前没有再发现“支付成功但本地漏认账”的直接证据
- 现在更重要的是：页面不要前后打架
- 这一点已经通过本轮状态投影和服务单验收去收了

---

## 5. 发现的问题清单

这轮一共确认到 2 类问题：

### 已确认并已修复
1. **退款到账后，前端还显示退款处理中**
   - 典型单：`332930560595366336`
2. **退款中的单混进普通服务状态桶**
   - 容易导致列表、角标、按钮互相打架
3. **后台详情可能展示错退款单**
   - 页面看的是旧退款，按钮操作的是新退款
4. **定时补偿顺序不对，可能先关单后查支付**
5. **退款通知过早 ACK，导致本地还没收口就不再重试**
6. **退款被拒绝文案不统一**
   - `已拒绝退款` / `退款已拒绝`

### 还需要继续盯着，但暂不算拦路大 bug
1. **历史老 trade 里，有不少还没留 payer_openid 快照**
   - 新代码已经开始补留
   - 老单靠现有 query/notify 快照和 oauth 兜底
2. **少量历史证据链不够漂亮**
   - 比如某些老 paid 单，远端已经 paid，但本地 query 证据不一定完整留到了 trade 快照里
   - 这个更像“留痕不够全”，目前不直接等于功能没通

---

## 5.1 子代对抗性审查后，我又补了两刀

子代 review 又揪出两条高风险口子，我没有只记账，已经顺手补了：

1. **自动关单前，先跳过还有虚拟支付待收口迹象的订单**
   - 防止‘微信那边其实付了，但本地先把单关了’
2. **定时补偿不再只盯正常单，也会看本地已取消/待取消但还没付清的虚拟支付单**
   - 防止一旦先关单，后面永远扫不到
3. **待收口保护不再死卡 10 分钟**
   - 改成更看 trade 还在不在未终态
   - 避免 10 分钟一过，又把‘继续支付 / 取消订单’放出来，继续把状态搅乱

所以现在这条链路，对‘晚通知、慢查单、多次尝试、先关单’这几类典型墨菲场景，比刚才又稳了一层。

---

## 6. 已修复的问题清单

本轮已经落地的修复，可以用大白话理解成：

- 改了“先关单后查支付”的顺序
- 改了支付待收口时的提示文案
- 改了退款通知的收口逻辑
- 改了退款完成时本地订单、退款单、trade 三边一起收口
- 改了查单/退款时 openid 证据来源
- 改了金额一致性校验
- 改了用户端和后台端对退款中的订单分桶
- 改了后台详情页选择当前有效退款单
- 改了 `退款已拒绝` 文案统一

---

## 7. 还需要你手动扫码/真机验证的清单

这些我建议你最后再配合走一遍。原因很简单：
- 这几步最依赖真实微信环境
- 纯本地脚本替不了

### 必做
1. **Android 开发版**
   - 下单 -> 虚拟支付成功
   - 看订单列表 / 订单详情 / 服务订单详情是否都一致
2. **Android 开发版**
   - 支付成功后申请退款
   - 商家审核退款
   - 退款到账后再看小程序前端是否显示 `已退款`
3. **Android 体验版**
   - 下单 -> 虚拟支付成功
   - 确认不再出现奇怪弹窗、状态错乱、还显示去支付
4. **Android 体验版**
   - 支付后退款
   - 看前端、后台、退款单状态是否一致

### 建议再补两条墨菲用例
5. **同一单快速重复点支付按钮**
   - 重点看会不会出现多次支付尝试后状态乱掉
6. **支付成功后立刻返回订单页连续刷新**
   - 重点看会不会短时间出现“已支付/待支付”来回跳

---

## 8. 改 和 不改 的优缺点

## 改了的好处
- 最直接：把“钱到了但本地没跟上”的高风险口子堵住
- 用户看到的状态、后台看到的状态，更一致
- 后面查问题更容易，因为证据链更多了
- 同类问题以后不太容易反复冒

## 改了的代价
- 支付链路比之前更严格了
- 多留了一些快照和校验逻辑
- 后续有人再改支付模块时，要更注意别把这套收口规则弄散

## 如果不改
### 好处
- 眼前省事
- 不用碰支付/退款这条敏感链路

### 坏处
- 你后面还会继续遇到同类问题
- 而且每次看起来像新 bug，本质其实是同一类“状态没收口”
- 越拖，历史脏单越多，越难排

我的判断：**这批改动值得改，而且应该改。**

---

## 9. 是否建议继续上线 / 提测

我的建议分两层：

### 如果是“继续提测”
**可以。**

因为：
- 自动化检查已经过了
- 主流程验收已经过了
- 数据库脏状态扫表这一轮也是干净的
- 你前面实机测出来的核心问题，这次已经补掉

### 如果是“直接正式上线”
**先别急，先补完最后一轮真机手动回归。**

尤其要盯：
- Android 开发版
- Android 体验版
- 支付后退款
- 退款到账后前端和后台是否同步显示完成

---

## 最终结论

现在这条虚拟支付链路，已经从“能跑，但很容易漏状态”，往前推进到了：

- **能跑**
- **多数异常能收口**
- **主状态基本统一**
- **后台和前台不再那么容易打架**

离“放心上线”还差最后一步：

> 再做一轮你配合的真机回归，重点复看支付成功、退款到账、页面状态同步。

如果你愿意，我下一步可以直接继续帮你出一份：

**《最后一轮手动验收清单（开发版 / 体验版 / 退款回归）》**

你就照着一条条点就行。

---

## 10. 今晚新增补丁：体验版支付后弹后端异常

### 现象
你刚在 Android 体验版上测到：
- 支付时先不付款，会弹错
- 再次支付，就算付成功了，也还会弹错

涉及交易号：
- `333001946691202244`
- `333002248341810559`

### 原因
这不是微信虚拟支付本身又挂了。
是我前面为了补“主动查单”时，写死了一个参数类型：
后端这里原本只肯收 **数组**，但实际传进来的是 **交易模型对象**，所以一查单就炸。

说白了就是：
**查单补偿逻辑写得太死，体验版一走到这里就报错弹窗。**

### 已处理
我已经把这个口子补掉了：
- 现在不管传进来的是数组，还是交易模型对象，都会先统一处理，再继续查单
- 不会再因为这个类型问题直接报错

### 已回归
我这边已经补跑：
- 支付链路本地回归：通过
- 小程序订单检查：通过

结果：
- `local virtual-payment checks ok: vplocal-20260707220339-525`
- `mp-weixin real-order checks ok`

### 你现在可以怎么复测
请你优先再测这两个动作：
1. Android 体验版下单，支付弹窗里先不付款
2. 再次发起支付，并完成支付

重点只看两件事：
- 还会不会再弹这个后端异常
- 支付成功后，订单状态会不会正常收口

### 当前判断
这个问题属于：
- **支付链路没断**
- **但主动查单补偿这里有个代码口子**

现在这个口子已经补了。
如果你复测不再弹错，这一条就算收掉。

---

## 11. 今晚新增补丁：用户没付款只是返回，不该被打成“支付确认中”

### 现象
你最新测到的是：
- 下单后拉起虚拟支付
- 人工返回上一级，不付款
- 前端弹“微信虚拟支付仍在处理中...”
- 服务订单详情还显示“支付确认中”

### 这次真正的问题
不是钱错了。
是系统把“你没付钱，只是从支付弹窗退出来”误判成了“支付还在确认”。

也就是两步连锁：
1. 前端把“取消/返回”也拿去自动查单
2. 后端又把 `0/1` 这种未付款状态，过度显示成“支付确认中”

### 我怎么改的
我这次不是硬改订单主状态，避免伤到真支付单。
而是把**判断口径**改正：

- 用户主动取消/返回：
  - 直接按“已取消支付”处理
  - 不再自动查单
- 后端订单展示：
  - 只有真出现“已付款证据”时，才显示待收口支付状态
  - 单纯 `0/1` 不再把前台后台都打脏

### 回归结果
我已经补跑：
- 本地虚拟支付链路回归：通过
- 用户端/后台/小程序订单检查：通过
- 服务单巡检：通过

关键结果：
- `local virtual-payment checks ok: vplocal-20260707222405-362`
- `h5 real-order checks ok`
- `backend real-order checks ok`
- `mp-weixin real-order checks ok`
- `service_suspicious: 0`

### 当前清理结果
你这个样本单：
- `333007081709494688`

现在已经回到干净口径里了，
不会再因为“没付款只是返回”而被前台/后台误显示成卡在支付确认里。
