# Operations/security verification — 2026-07-15

## Live foundation state

- UFW active, default incoming/routed deny; only TCP 22/80/443 allowed for IPv4/IPv6.
- Fail2ban `sshd` jail active.
- Effective SSH: public key enabled; password and keyboard-interactive disabled; root is key-only temporarily; max auth tries 3.
- `/etc/sudoers` and restricted `yoshop-deployer` file parse cleanly. `deployer` can call release status but arbitrary sudo is denied.
- Root/deployer authorized-key files are mode 0600 under mode-0700 `.ssh`; both still intentionally contain the same temporary migration key. Rotation remains manual/deferred to the delivered guide.
- Nginx, MySQL, Redis active; no failed systemd units; Timer inactive/disabled until an authorized release.
- Disk 13%, journal about 456 MiB, no Workerman log files.
- Journald bounds: 512 MiB, 2 GiB free-space reserve, 14-day retention, rate limiting.
- Release logrotate parses and retains 14 rotations at a 20 MiB size threshold.

## MySQL durability/retention

MySQL 8.0.46 already used row binlogs with `sync_binlog=1` and `innodb_flush_log_at_trx_commit=1`. Binlog expiry was 30 days, which did not match the reviewed 7-day disk budget. A tracked `deploy/mysql/60-yoshop-retention.cnf` was installed; `mysqld --validate-config` passed and the runtime value was persisted to 604800 seconds. No MySQL restart was needed.

## Certbot defect found and corrected

The first renewal dry run found:

- `wx.gxwqb.cn`: success;
- `www.gxwqb.cn`: failure (403), because the static-site Nginx block denied `/.well-known`.

After adding an explicit `^~ /.well-known/acme-challenge/` route rooted at `/var/www/html`, an external no-proxy probe returned 200 for both `www.gxwqb.cn` and `gxwqb.cn`. A second dry run then failed with 404, revealing a second stale dependency: `/etc/letsencrypt/renewal/www.gxwqb.cn.conf` still wrote challenges to the deleted old path `/mnt/vps/tencent/Nginx`.

The renewal file was backed up and changed to `/var/www/html`. A targeted `certbot renew --cert-name www.gxwqb.cn --dry-run --no-random-sleep-on-renew` then passed. The earlier targeted wx dry run had already passed. The deploy reload hook exists, is root-owned/executable, and passes `bash -n`; `certbot.timer` is enabled and active.

Both production and maintenance Nginx templates now preserve ACME routes for the wx host and filing static hosts. New contract tests cover these routes and the maintenance-vs-health behavior. The active server remains on maintenance 503 for B; filing hosts continue returning 200.

## Deferred checks

- Daily production backup/restore cannot run against the final DB until the production import authorization gate.
- Timer stability/crash-storm observation and live code rollback require an authorized active release.
- COS/CAM/offsite backup and external failure notifications remain explicitly deferred; current local-disk backups cannot satisfy host-loss disaster recovery.

A final unfiltered `certbot renew --dry-run --no-random-sleep-on-renew` passed both `www.gxwqb.cn` and `wx.gxwqb.cn` in one run after the fixes.

## Timer immutable-release hardening and final local check

After `Timer.php` moved Workerman PID/log state into `shared/runtime/workerman`, the service sandbox was tightened from `ProtectSystem=full` to `ProtectSystem=strict`; `/srv/yoshop/shared` remains the only application write exception. The stale test comment claiming that Workerman wrote into the release was replaced with the actual shared-runtime contract. The backup unit's invalid `Documentation=file:/srv/yoshop/current/deploy/...` pointer was removed because release archives intentionally exclude `deploy/`.

The updated Timer unit was installed on Tencent and `daemon-reload` completed. Its SHA-256 matched the local tracked unit (`04ac6ef239e403202c5dfa82b726f7f934cf103e1f73ac3d82a17bad8c01b45f`). It remains `disabled` and `inactive`, with no `/srv/yoshop/current`. Tencent's systemd 249 reports exposure `1.4 OK`; its `systemd-analyze verify` emitted only unrelated pre-existing warnings for `tat_agent` and `snapd`. The host version does not support `systemd-analyze security --offline=yes`, so the loaded-unit form was used instead.

Full local verification after these changes:

- deploy Python suite: 24/24 passed;
- production initializer suite: 12/12 passed;
- release prepare/activate/install/rollback integration fixture: passed;
- atomic backup/retention/manifest/guarded-restore fixture: passed;
- Timer shared runtime PHP contract: 22 checks passed;
- `bash -n` and ShellCheck over all deploy shell entry points: passed;
- Python byte compilation: passed;
- changed Timer PHP lint: passed;
- repository `git diff --check`: passed;
- Timer unit offline exposure on the local systemd: `1.4 OK`.

The backup unit and timer remain uninstalled/unstarted on Tencent because the final production DB and protected shared configuration do not exist yet. Installing them early would create a misleading green timer with no valid backup target. Real daily backup, temporary-DB restoration proof, Timer observation and live rollback remain coupled to the separately authorized production cutover.
