# 实施计划：Feedback and complaint MVP

## 执行原则

- 严格保持在单向工单 MVP 范围内。
- 不扩展成聊天、消息中心、通知中心。
- 实现前仍需用户明确批准并执行本子任务自己的 `task.py start`。

## 阶段 1：数据层与接口骨架

- [x] 新增反馈表 SQL / 安装记录。
- [x] 新增 issue type / status 枚举。
- [x] 新增 common / api / store 三侧 feedback model / service / controller 骨架。

验证：
- 数据表结构满足主记录 + 回复 + 状态流转需要；
- 基础枚举能覆盖投诉反馈场景。

## 阶段 2：小程序用户侧

- [x] 实现 `create/list/detail` 接口。
- [x] 新增 `pages/feedback/index.vue`。
- [x] 新增 `pages/feedback/detail.vue`。
- [x] 在 `pages/user/index.vue` 增加反馈入口（当前文案：`投诉入口`）。
- [x] 在 `pages.json` 注册反馈相关页面。
- [x] 复用现有 upload API，完成图片上传与表单校验。

验证：
- 缺 issue type / content / mobile 时会拦截；
- 正常提交后能在记录列表看到新记录；
- 详情页能看到状态与官方回复字段。

## 阶段 3：商家后台处理页

- [x] 新增反馈列表页。
- [x] 新增详情或编辑抽屉/弹窗。
- [x] 支持状态更新与官方回复编辑。
- [x] 提供 `投诉反馈` 快捷筛选或标签。
- [x] 补 router / menu / permission 接入。

验证：
- 后台可筛出投诉反馈；
- 修改状态或回复后，小程序记录页能看到变化。

## 阶段 4：共享文件协调门槛

- [x] 已吸收共享文件最新版本并在其上叠加反馈入口与页面注册。
- [x] 已与首页弹窗 / 隐私协议改动对齐，避免覆盖共享文件。

## 回滚点

- 最快回滚：隐藏前台反馈入口、隐藏后台菜单与路由。
- 模块回滚：停用 feedback API / 页面，不影响首页与订单主链路。
- 数据层回滚：反馈表与记录可保留，不需要强制清理。

## 启动前检查

- [x] 用户已明确批准启动并按默认推荐执行
- [x] Child B `prd.md` / `design.md` / `implement.md` 已 review
- [x] `pages/user/index.vue` / `pages.json` 的共享文件协调已确认
- [x] 当前已直接执行修复与交付，未再沿用 planning 约束


## 本轮实际交付（2026-07-10）

- 修复小程序反馈图片上传在微信端因 `tempFilePath/path` 归一化不完整导致的 `uploadFile:fail parameter`。
- 小程序用户中心入口文案调整为 `投诉入口`。
- 商家后台新增 `内容编辑 -> 首页首登弹窗与隐私协议` 页面，并将可编辑内容从 `客户端小程序设置` 中迁出。
- 商家后台补齐反馈/投诉菜单、接口绑定与页面访问路径。
- 已完成小程序构建、商家后台构建与静态资源部署。

## 已执行验证（2026-07-10）

- 商家后台目标文件 lint 通过。
- PHP 相关控制器 / 模型语法检查通过。
- 编译后 mp-weixin 产物已确认包含上传修复与按钮文案。
- 已校验 `public/store` 为最新构建产物，并确认菜单/路由落点存在。

## 待人工验收

- 微信开发者工具重新打开最新 `mp-weixin` 编译目录后，验证图片上传与 `投诉入口` 文案。
- 商家后台强刷后验证：
  - `内容管理 -> 反馈/投诉`
  - `店铺管理 -> 内容编辑 -> 首页首登弹窗与隐私协议`
  - `客户端 -> 微信小程序 -> 小程序设置` 不再承载上述两个内容编辑项。
