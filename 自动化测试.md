# 自动化测试

## 这套自动化现在测什么

当前 `service-order:e2e` 已经改成 **普通微信支付 真实服务商品下单流** 非虚拟支付，不再是“克隆历史订单再校验状态”的旧逻辑。

现在默认会使用：

- 当前商城里 **已上架的真实服务商品**
- debug 测试用户：`19900000000`
- 真实下单接口：`/api/checkout/submit?mode=buyNow`
- 真实支付接口：`/api/cashier/orderPay`
- 后台真实服务流接口：
  - `startService`
  - `completeService`
  - `refundBeforeService`

## 当前覆盖场景

一次 `check` 会自动创建并校验这 4 类订单：

1. **未支付订单**
   - 验证用户能下单成功
   - 验证订单状态是 `pending_payment`
   - 验证用户可以取消

2. **已支付待联系订单**
   - 验证余额支付成功
   - 验证订单状态是 `pending_contact`
   - 验证服务联系信息、备注、订单详情正常

3. **服务前退款订单**
   - 验证后台 `refundBeforeService` 能成功执行
   - 验证最终订单状态变成 `refunded`

4. **服务完成订单**
   - 验证后台 `startService`
   - 验证后台 `completeService`
   - 验证最终订单状态变成 `completed`

另外：

- H5 会校验真实订单详情状态
- 后台会校验真实订单动作权限和状态
- 小程序目前仍是 **真实订单 + MP-WEIXIN 平台接口校验**
  - 不是点开微信开发者工具做 UI 自动点击
  - 但已经不再是历史 fixture 订单

## 用法

改完代码后，按下面顺序执行。

## 1. 先看计划

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e plan --target h5,mp-weixin,backend --dry-run
```

如果你想指定测试商品，建议显式带上：

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e plan \
  --target h5,mp-weixin,backend \
  --mode real \
  --service-goods-id 10006 \
  --service-goods-sku-id 0 \
  --min-balance 200 \
  --dry-run
```

## 2. 跑自动化检查

### H5 + 后台

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e check \
  --mode real \
  --check h5,backend \
  --service-goods-id 10006 \
  --service-goods-sku-id 0 \
  --min-balance 200 \
  --evidence-dir "/opt/yoshop/runtime/service-order-e2e"
```

### 小程序

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e check \
  --mode real \
  --check mp-weixin \
  --service-goods-id 10006 \
  --service-goods-sku-id 0 \
  --min-balance 200 \
  --evidence-dir "/opt/yoshop/runtime/service-order-e2e"
```

### 一次跑三端

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e check \
  --mode real \
  --check h5,mp-weixin,backend \
  --service-goods-id 10006 \
  --service-goods-sku-id 0 \
  --min-balance 200 \
  --evidence-dir "/opt/yoshop/runtime/service-order-e2e"
```

## 3. 清理测试数据

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e cleanup
```

## 常用单项命令

### 只跑 H5

```bash
php think service-order:e2e check --mode real --check h5 --service-goods-id 10006 --service-goods-sku-id 0
```

### 只跑后台

```bash
php think service-order:e2e check --mode real --check backend --service-goods-id 10006 --service-goods-sku-id 0
```

### 只跑小程序

```bash
php think service-order:e2e check --mode real --check mp-weixin --service-goods-id 10006 --service-goods-sku-id 0
```

### 只生成测试计划

```bash
php think service-order:e2e plan --target h5,mp-weixin,backend --mode real --service-goods-id 10006 --service-goods-sku-id 0 --dry-run
```

## 失败证据

- 目录：`/opt/yoshop/runtime/service-order-e2e`
- 会自动保存：
  - `*.json` 日志
  - `*.png` 占位截图

## 重要说明

- debug 登录已内置
- 默认测试身份固定为：`19900000000`
- 会自动给 debug 用户补足余额到 `--min-balance`
- 默认优先跑 **real 模式**
- `cleanup` 现在会清掉：
  - 旧版 legacy fixture 订单
  - 本轮真实下单生成的测试订单
- 测试结束后必须执行 `cleanup`
- 如果当前环境已关闭 `loginDebug`，这套自动化可能在登录或后续支付阶段失真
- 一旦出现 `当前接口仅调试环境可用` 或 `订单不存在` 这类链路错误，应先视为“当前自动化基线不可作验收证据”

## 当前边界

当前已经覆盖：

- 下单成功
- 未支付
- 余额支付成功
- 服务前退款
- 开始服务
- 完成服务

当前还没覆盖：

- 第三方微信支付真实回调链路
- 微信小程序虚拟支付 `wx.requestVirtualPayment` 真正拉起
- 微信虚拟支付通知 URL `GET/echostr` 校验
- 微信虚拟支付支付通知 / 查单 / 退款 / 履约回推闭环
- 用户支付后主动申请退款
- 用户未支付超时自动关闭
- 更复杂的多商品 / 多 SKU / 异常库存并发场景
- 小程序端真实 UI 自动点击

## 关于 `mp-weixin` 检查的说明

当前 `php think service-order:e2e check --check mp-weixin`：

- 只能作为“小程序目录、接口、服务单详情状态”的接口视角检查
- 不能作为“虚拟支付已验收”的证明
- 不会真实拉起 `wx.requestVirtualPayment`
- 不会自动验证微信虚拟支付通知、查单、退款、履约回推

如果要验收虚拟支付，仍需以真实沙箱小程序支付闭环为准。

这些要补的话，应该继续在这套 `real` 流程基础上往前扩，不要再回退到历史订单克隆模式。

## 推荐顺序

1. `plan`
2. `check`
3. `cleanup`
