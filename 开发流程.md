# 商城 uni-app 开发流程（WSL + Windows 双环境版）

> 适用场景：后续每次继续做同一套 uni-app 二次开发时，直接按本流程执行。
>
> 核心原则：**WSL 只负责改源码和编译 H5，Windows 对应目录只负责编译微信小程序。**

---

## 1. 当前约定

### 1.1 源码主目录

- **WSL 源码主目录**：`/opt/yoshop/yoshop2.0-uniapp`
- 所有前端修改都只在这里完成
- 不建议在 Windows 镜像目录里直接改源码

### 1.2 H5 预览目录

- H5 构建输出目录：`/opt/yoshop/yoshop2.0-uniapp/dist/build/h5`
- 站点服务目录：`/opt/yoshop/yoshop2.0/public`
- 浏览器预览地址：`http://localhost/#`

### 1.3 微信小程序编译目录

- Windows 对应项目目录：`D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp`
- 微信小程序产物目录：`unpackage/dist/dev/mp-weixin`
- 微信开发者工具导入这个产物目录即可

### 1.4 当前已验证的编译脚本

- WSL / H5：`npm run build:h5:sync`
- Windows / 微信小程序：`npm run build:mp-weixin`
- WSL 一键联动：`npm run dev:all`
- 微信小程序编译时，**不要**让 Windows 工具直接盯 WSL 的 `\wsl.localhost\...` 路径
- 当前稳定状态：
  - H5 在 WSL 编译后同步到 `public`
  - 微信小程序在 Windows 镜像目录编译
  - 微信开发者工具只打开 `unpackage/dist/dev/mp-weixin`

---

## 2. 一句话流程

```text
WSL 改源码 → WSL 编译 H5 → 浏览器预览 → 同步到 Windows 目录 → Windows 目录编译微信小程序 → 微信开发者工具验证
```

---

## 3. 每次开发的标准步骤

### Step 1：在 WSL 中修改源码

只改主目录里的源码：

- 页面逻辑
- 接口调用
- 样式
- 条件编译
- 配置文件

尽量不要直接改 Windows 镜像目录。

---

### Step 2：在 WSL 中编译 H5

在 `/opt/yoshop/yoshop2.0-uniapp` 下执行：

```bash
npm run build:h5:sync
```

这个命令会做两件事：

1. 编译 H5
2. 把 H5 产物同步到 `public` 目录

---

### Step 3：浏览器预览

打开：

```text
http://localhost/#
```

检查：

- 页面是否是最新代码
- 样式是否正常
- 接口是否正常
- 关键交互是否正常

如果不对，先回 WSL 改源码，再重新执行 H5 编译。

---

### Step 4：同步源码到 Windows 对应目录

把 WSL 里改好的前端源码同步到 Windows 对应目录：

```text
D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp
```

说明：

- 这是**单向同步**
- 只允许 **WSL → Windows**
- Windows 目录只作为小程序编译副本

### Step 4.1：也可以用一键脚本直接联动同步

在 WSL 主目录执行：

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run dev:all
```

这个命令会依次做：

1. 同步 WSL 源码到 Windows 镜像目录
2. 编译 H5 并同步到 `public`
3. 编译微信小程序到 `unpackage/dist/dev/mp-weixin`

---

### Step 5：在 Windows 对应目录编译微信小程序

在 Windows 镜像目录中执行：

```bash
npm run build:mp-weixin
```

注意：

- 这个命令会先自动拉起 **HBuilderX**，再继续编译
- 如果 HBuilderX 还没启动，脚本会先执行 `cli open`，然后继续 `launch mp-weixin`
- 脚本里用的是 Windows 镜像目录的绝对路径，不要改成 WSL 路径
- 这是为了避免 DevTools 继续吃旧产物
- 实际执行时，脚本是通过 WSL 里的 `build-mp-weixin.cjs` 去调用 Windows 侧的 `HBuilderX/cli.exe`

编译产物会输出到：

```text
unpackage/dist/dev/mp-weixin
```

注意：

- 这里必须是 **Windows 原生目录** 下的项目副本
- 不要在 WSL 根目录里直接调用 Windows 的微信开发者工具/编译器去盯 `\wsl.localhost\...`
- 否则容易出现 `EISDIR`、watch 失败、空白页等问题

---

### Step 6：微信开发者工具验证

在微信开发者工具里导入：

```text
D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin
```

检查：

- 页面是否打开正常
- 文案是否正确
- 条件编译是否生效
- 关键流程是否可走通

---

## 3.1 按场景执行的最简流程

### 场景 A：只改了 WSL / uni-app 前端

适用内容：

- H5 页面
- 微信小程序页面
- uni-app 公共组件
- 前端接口调用
- 前端样式和条件编译

执行：

1. 在 WSL 主目录修改源码
2. 如果要看 H5，执行 `npm run build:h5:sync`
3. 如果要看微信小程序，先同步到 Windows 镜像目录
4. 在 Windows 镜像目录执行 `npm run build:mp-weixin`
5. 微信开发者工具打开 `unpackage/dist/dev/mp-weixin`

不需要：

- 清后端缓存
- 重载 PHP

### 场景 B：只改了后端 PHP

适用内容：

- 接口控制器
- 模型
- 服务层
- 路由
- 配置读取
- 后端缓存逻辑

执行：

1. 修改后端 PHP 代码
2. 如有需要，先清理后端缓存
3. 重载 PHP 进程（`php8.3-fpm`）
4. 如后端页面产物也变了，再重新发布 `public/admin` / `public/store`

一般不需要：

- H5 编译
- 微信小程序编译

### 场景 C：前端 + 后端同时修改

适用内容：

- 前端页面改了
- 后端接口也改了
- 前端和后端都涉及发布结果

执行：

1. 先改前端和后端源码
2. 前端按场景 A 走：H5 预览、同步、微信小程序编译
3. 后端按场景 B 走：清缓存、重载 PHP
4. 如果 admin / store 前端产物也变了，再把编译结果发布到后端 `public/admin`、`public/store`

### 场景 D：只想让 H5 或小程序看新效果

执行：

- H5：`npm run build:h5:sync`
- 小程序：同步源码到 Windows 镜像目录后，执行 `npm run build:mp-weixin`

不需要：

- 清后端缓存
- 重载 PHP

---

## 4. 本次沉淀下来的可复用命令

### H5 编译 + 同步

```bash
npm run build:h5:sync
```

### 微信小程序编译

```bash
npm run build:mp-weixin
```

### 一键同步编译

```bash
npm run dev:all
```

### 微信开发者工具打开目录

```text
D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin
```

### 浏览器预览

```text
http://localhost/#
```

---

## 5. 本次踩坑总结

### 5.1 不能把 WSL 和 Windows 当成两份独立源码

正确理解：

- WSL 是主源码
- Windows 目录是同步后的编译副本

不要两个目录都各自改源码，否则很容易出现：

- 改动丢失
- 版本不一致
- 预览和小程序内容不一致

---

### 5.2 H5 和小程序不是同一个构建目标

H5 主要用于：

- 浏览器快速预览
- 检查页面效果

微信小程序主要用于：

- 平台兼容验证
- 最终在微信开发者工具里确认效果

所以每次开发都应该两边都看一遍。

---

### 5.3 Windows 目录编译前，先确保源码已同步

如果 Windows 目录里还是旧代码，可能出现：

- 小程序还是旧页面
- H5 已经是新页面，但小程序不是
- 你会误以为是构建问题，其实是同步没到位

---

### 5.4 如果构建异常，先判断是哪个环节出问题

先按这个顺序排查：

1. 源码是否真的改对了
2. H5 是否真的成功构建
3. `public` 是否已经同步成功
4. Windows 目录是否已经同步成功
5. 微信小程序是否在正确目录构建

不要一上来就同时怀疑所有环节。

### 5.4.1 什么时候需要清后端缓存 + 重载 PHP

只有后端真的变了，才做这两步：

- 改了 PHP 控制器 / 模型 / service
- 改了路由、配置、缓存逻辑
- 改了会被后端进程长期持有的代码
- 改了后端发布后的静态资源，并且需要立刻生效

如果只是：

- 改 uni-app 前端
- 只做 H5 预览
- 只做微信小程序预览

通常不需要清缓存，也不需要重载 PHP。

### 5.6 Windows 小程序编译的实际工作流

1. 在 WSL 主目录修改源码
2. 运行 `npm run dev:all` 或先手动同步到 Windows 镜像目录
3. 运行 `npm run build:mp-weixin`
4. 脚本会自动拉起 Windows 侧 HBuilderX 并编译
5. 打开微信开发者工具，导入 `unpackage/dist/dev/mp-weixin`
6. 如果看到旧内容，先重开项目，再看控制台日志

---

### 5.5 小程序空白页的优先排查顺序

如果微信开发者工具里打开后是空白，先按这个顺序看：

1. 产物是不是从 **Windows 镜像目录** 编出来的
2. 有没有把 WSL 的 `\wsl.localhost\...` 路径直接交给 Windows 编译器
3. `App.vue` 启动阶段有没有报错
4. 首页 API 失败是否已经被兜底，不至于直接把页面打空
5. 微信里打开的是否真的是最新 `unpackage/dist/dev/mp-weixin`
6. 首屏是否因为加载态/主题样式/隐私弹窗这类初始化逻辑卡住
   - 先让页面能显示“首页加载中...”
   - 再让数据回来后显示实际组件
   - 不要让首屏一开始就挂复杂组件树

---

## 6. 每次开发前的检查清单

- [ ] 当前改动只在 WSL 主目录
- [ ] H5 编译命令可执行
- [ ] `http://localhost/#` 可访问
- [ ] Windows 对应目录已同步最新源码
- [ ] 微信小程序编译命令可执行
- [ ] 微信开发者工具导入的是最新产物目录

---

## 7. 每次开发后的最小复盘模板

```md
## 本次改了什么
- 

## H5 是否正常
- 

## 小程序是否正常
- 

## 遇到的问题
- 

## 解决方式
- 

## 下次要注意什么
- 
```

---

## 8. 这次已经确认的关键修复点

- 首页从“直接挂 Page 组件”改为“先显示加载态，再渲染内容”
- `appThemeStyle` 做了空值兜底，避免主题数据未就绪时白屏
- `App.vue` 的小程序更新逻辑增加了环境判断
- 隐私弹窗的 `getPrivacySetting` 增加了兜底，不让初始化阶段直接炸掉
- 请求错误不再直接抛出到启动链路
- 微信小程序编译改为 Windows 原生目录，不再直接吃 WSL 路径
- 微信小程序编译脚本已改为先打开 HBuilderX，再执行 compile，并在未打开时直接失败

---

## 9. 最终原则

> **WSL 负责开发和 H5 预览，Windows 目录负责微信小程序编译。**
>
> **只允许单向同步，不允许双向改源码。**

---

## 10. 日常最短操作卡片

### 改完代码后先看 H5

```bash
cd /opt/yoshop/yoshop2.0-uniapp
npm run build:h5:sync
```

浏览器打开：

```text
http://localhost/#
```

### 再看微信小程序

1. 直接执行：

```bash
npm run build:mp-weixin
```

2. 脚本会自动拉起 Windows 端 HBuilderX，再继续编译
3. 需要一键联动时，在 WSL 主目录直接执行：

```bash
npm run dev:all
```

4. 微信开发者工具导入：

```text
D:\Program\0\home\0\yoshop1\yoshop2.0-uniapp\unpackage\dist\dev\mp-weixin
```

### 如果页面还是旧的

- 先看编译日志里有没有 `项目 yoshop2.0-uniapp 编译成功`
- 再看 `unpackage/dist/dev/mp-weixin` 的时间戳是否更新
- 还不行就重启微信开发者工具并清缓存

---

## 11. 服务单自动化本地运行

### 11.1 单入口命令

后端项目目录执行：

```bash
cd /opt/yoshop/yoshop2.0
php think service-order:e2e plan --target h5,mp-weixin,backend --mode real --dry-run
```

这个命令会输出三类目标的统一执行计划。现在默认应该按 **real 模式** 使用：

- H5
- 微信小程序
- 后台

说明：

- `real` = 用当前上架真实服务商品，走真实下单 / 支付 / 服务流接口
- `legacy` = 旧版历史订单克隆校验模式，只建议排查兼容问题时临时用
- 正常本地回归，统一用 `real`

### 11.2 常用参数

```bash
php think service-order:e2e plan \
  --target all \
  --mode real \
  --h5-url http://localhost/ \
  --backend-url http://localhost/store/ \
  --backend-user admin \
  --backend-pass yinghuo \
  --service-goods-id 10006 \
  --service-goods-sku-id 0 \
  --min-balance 200 \
  --wechat-cli /mnt/d/Program/soft/wechattools/cli.bat \
  --hbuilderx-cli /mnt/d/Program/tools/HBuilderX/cli.exe \
  --dry-run
```

说明：

- `--target` 支持：`all`、`h5`、`mp-weixin`、`backend`
- 多目标可写成：`--target h5,backend`
- `--mode` 支持：`real`、`legacy`、`all`
- `--service-goods-id` / `--service-goods-sku-id` 建议显式指定真实服务商品
- `--min-balance` 用于确保 debug 用户余额足够完成余额支付
- 只看计划用 `plan`
- 真正执行建议直接用 `check`
- 只想看命令展开但不真正执行，可加 `--dry-run`

### 11.3 只跑单个目标

只跑 H5：

```bash
php think service-order:e2e check --mode real --check h5 --service-goods-id 10006 --service-goods-sku-id 0
```

只跑小程序：

```bash
php think service-order:e2e check --mode real --check mp-weixin --service-goods-id 10006 --service-goods-sku-id 0
```

只跑后台：

```bash
php think service-order:e2e check --mode real --check backend --service-goods-id 10006 --service-goods-sku-id 0
```

### 11.4 调试登录身份

```bash
php think service-order:e2e identity --identity debug --token
```

当前稳定测试身份：
- 调试登录接口：`/passport/loginDebug`

### 11.5 当前本地路径约定

- uni-app：`/opt/yoshop/yoshop2.0-uniapp`
- 后端：`/opt/yoshop/yoshop2.0`
- 后台前端：`/opt/yoshop/yoshop2.0-store`
- 微信开发者工具 CLI：`/mnt/d/Program/soft/wechattools/cli.bat` 
- 微信开发者工具 端口号:14859
- HBuilderX CLI：`/mnt/d/Program/tools/HBuilderX/cli.exe`

### 11.6 推荐执行顺序

1. 先跑 `php think service-order:e2e plan --target h5,mp-weixin,backend --mode real --dry-run`
2. 确认 URL、账号、Windows CLI 路径没写错
3. H5 先确保 `http://localhost/` 能打开
4. 小程序先确保 Windows 镜像目录已同步
5. 再跑 `php think service-order:e2e check --mode real --check h5,mp-weixin,backend --service-goods-id 10006 --service-goods-sku-id 0 --evidence-dir "/opt/yoshop/runtime/service-order-e2e"`
6. 跑完后执行 `php think service-order:e2e cleanup`
